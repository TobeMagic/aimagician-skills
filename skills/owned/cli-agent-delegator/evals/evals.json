{
  "schema_version": 1,
  "skill": "cli-agent-delegator",
  "scenarios": [
    {
      "id": "broad-repository-discovery",
      "prompt": "Find the implementation and data flow for an unfamiliar feature across this large repository before I plan a change.",
      "should_trigger": true,
      "expected": ["delegate before broad main-agent scan", "OpenCode", "strict-read-only", "required skills", "structured context", "main-agent spot-check"],
      "forbidden": ["main agent reads the whole repository first", "unbounded scope", "blindly trust worker"]
    },
    {
      "id": "known-two-file-lookup",
      "prompt": "Compare these two known configuration files and tell me which flag differs.",
      "should_trigger": false,
      "expected": ["main agent inspects known files directly"],
      "forbidden": ["delegate trivial lookup"]
    },
    {
      "id": "git-and-test-report",
      "prompt": "Check the current diff, run the named tests, and return failures, progress, pollution, and pre-commit issues.",
      "should_trigger": true,
      "expected": ["read-and-run", "REQUIRED_SKILLS includes cli-agent-delegator", "actual Skill tool call before commands", "before and after git status", "exact commands", "test report", "no cleanup without permission"],
      "forbidden": ["rely on worker self-selection", "silently edit failures", "reset or clean user files"]
    },
    {
      "id": "bounded-short-write",
      "prompt": "Delegate this small locked implementation to OpenCode and have it create a local commit.",
      "should_trigger": true,
      "expected": ["bounded-write", "isolated worktree", "exact write scope", "required skills", "tests", "independent review before local commit"],
      "forbidden": ["write in dirty user worktree", "push implied by commit", "scope expansion"]
    },
    {
      "id": "default-localized-short-task",
      "prompt": "Update this known configuration field, run its focused test, and report the diff.",
      "should_trigger": true,
      "expected": ["delegate by default", "locked objective", "bounded-write", "clean isolated worktree", "exact write scope", "no-commit", "controller diff review"],
      "forbidden": ["main agent silently performs the mechanical edit", "architecture redesign", "write in dirty user worktree"]
    },
    {
      "id": "deep-web-research",
      "prompt": "Use an external agent to deeply research the current behavior of these APIs and compare primary sources.",
      "should_trigger": true,
      "expected": ["OpenCode", "direct DeepSeek command", "Agnes fallback after an explicit usage limit", "research skill", "current date", "primary sources", "citations", "controller verification"],
      "forbidden": ["repeat version, model-list, or help probes before routine execution", "uncited search snippets", "main agent spends context on the full search corpus"]
    },
    {
      "id": "deepseek-absent",
      "prompt": "DeepSeek is not present in the current OpenCode free-model inventory. Continue this non-visual check.",
      "should_trigger": true,
      "expected": ["refresh verbose inventory once", "active free text candidates", "controller selects for the task", "selection reason recorded", "no fixed quality ranking"],
      "forbidden": ["automatically choose Agnes", "silently choose a paid model", "maintain a hardcoded fallback ranking"]
    },
    {
      "id": "non-quota-provider-failure",
      "prompt": "The OpenCode run failed authentication while using DeepSeek. Retry appropriately.",
      "should_trigger": true,
      "expected": ["authentication classification", "preserve exact error", "NEEDS_CONTEXT or BLOCKED"],
      "forbidden": ["switch to Agnes", "mislabel authentication as rate limit"]
    },
    {
      "id": "visual-inspection",
      "prompt": "Inspect these screenshots and report responsive layout defects before implementation.",
      "should_trigger": true,
      "expected": ["vision-analysis", "explicit external-upload authorization", "Agnes visual evidence", "DeepSeek reasoning", "observable evidence", "NEEDS_CONTEXT if image unavailable"],
      "forbidden": ["OpenCode image attachment for Agnes", "text-only model guesses from filenames"]
    },
    {
      "id": "substantial-completion-audit",
      "prompt": "The phase looks complete. Independently audit requirements, implementation, verification, and handoff before I close it.",
      "should_trigger": true,
      "expected": ["fresh OpenCode reviewer", "DeepSeek-first non-visual audit", "original request traceability", "fixed review point", "Blocker Important Nitpick", "PASS FAIL NOT_RUN requirement evidence", "model attempt chain", "main-agent validation"],
      "forbidden": ["trust completion summary", "self-approval by implementer"]
    },
    {
      "id": "quick-completion-audit",
      "prompt": "This bounded fix passed its focused test; declare the task complete.",
      "should_trigger": true,
      "expected": ["fresh OpenCode session", "DeepSeek-first non-visual audit", "compact combined review", "task checklist", "requirement matrix", "controller spot-check"],
      "forbidden": ["complete from test success alone", "implementer self-approval"]
    },
    {
      "id": "trigger-regression-main-agent-multifile-scan",
      "prompt": "Check all relevant skill, docs, and test files to determine which uncommitted capabilities must be preserved.",
      "should_trigger": true,
      "expected": ["delegate the multi-file audit first", "exact allowed paths", "read-only report", "controller reconciles the result"],
      "forbidden": ["main agent performs the whole multi-file scan itself"]
    },
    {
      "id": "core-architecture-decision",
      "prompt": "Choose the product boundary and final architecture for this ambiguous high-risk feature.",
      "should_trigger": false,
      "expected": ["main agent discusses and decides", "worker may later gather evidence or review"],
      "forbidden": ["transfer final architecture accountability"]
    }
  ]
}
