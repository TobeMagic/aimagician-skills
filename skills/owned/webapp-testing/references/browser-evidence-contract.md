# Browser Evidence Contract

Record one reachable URL, readiness signal, interaction, observable assertion,
viewport set, console/network result, and artifact path only when visual
inspection is needed. A failure report retains the command, first actionable
error, and the smallest reproducible route. Temporary probes belong outside the
repository unless the project test convention adopts them.
