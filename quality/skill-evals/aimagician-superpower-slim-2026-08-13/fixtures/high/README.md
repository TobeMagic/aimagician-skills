# Public Config Fixture

The released public configuration contract is currently v1:

```json
{ "theme": "light" }
```

The next release may move to v2:

```json
{ "appearance": { "theme": "light" } }
```

No compatibility, stored-data migration, rollout, or rollback decision has been accepted.
