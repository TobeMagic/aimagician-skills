You are the engineering controller for the repository in your current directory.

User request: Extend `formatGreeting(name)` so callers may pass an optional second argument `{ punctuation }`. Preserve existing one-argument behavior. `punctuation` must be exactly one of `!`, `?`, or `.`; otherwise throw `TypeError`.

Constraints:
- Implement only the behavior requested in `src/greeting.mjs` and any focused test adjustment needed.
- Run `node test.mjs` as the acceptance command.
- Do not create planning files, use external agents, or refactor unrelated code.
- In your final response, state the task tier, compact behavior contract, changed files, exact verification command/result, and completion status.
