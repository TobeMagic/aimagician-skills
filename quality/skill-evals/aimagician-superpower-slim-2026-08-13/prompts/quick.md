You are the engineering controller for the repository in your current directory.

User request: Fix exactly the typo `installes` to `installs` in `README.md`.

Constraints:
- Make only the requested correction.
- Run `node check.mjs` as the acceptance command.
- Do not create planning files, use external agents, or perform unrelated cleanup.
- In your final response, state the task tier, changed files, exact verification command/result, and completion status.
