## Task Tier

**Tier 3 — Blocked | High-risk contract migration.** Migrating a *released public* config contract is a breaking, consumer-facing change touching stored data and rollout/rollback. It sits outside the current session's decision authority and cannot be implemented as specified.

## Source Facts

- `README.md:3-13` — Released contract is **v1** `{ "theme": "light" }`; v2 would be `{ "appearance": { "theme": "light" } }`. Explicitly states: *no compatibility, stored-data migration, rollout, or rollback decision has been accepted.*
- `src/config.mjs:1-3` — Only implementation: `readTheme(config) { return config.theme; }`. Reads the **flat v1 shape**; under a naive v2 payload it would return `undefined`.
- No tests, no other sources, not a git repo. No migration/loader/compat code exists anywhere.

## Blocking Ambiguity

The request says "migrate now," but the repository declares none of the migration prerequisites are decided. A v2 flip without those decisions either (a) breaks every v1 consumer reading `config.theme`, or (b) silently drops persisted v1 data — both cross the explicit "not accepted" line. The migration itself is well-defined; **the decision outcome is not**.

## Must Be Decided Before Implementation

1. **Compatibility** — Breaking (v1 payloads rejected) vs. backward-compatible dual-read (`config.theme` fallback)? Which for chart, which for read path?
2. **Stored-data migration** — How persisted v1 objects become v2: write-time upgrade, read-time normalization, or background rewrite; what idempotency/atomicity contract?
3. **Rollout** — Single-version cutover vs. dual-write/deprecation window; version signaling for consumers?
4. **Rollback** — What trigger/process restores v1, and how is a v2-written store reverted safely?
5. **Scope consent** — The "do not modify files" constraint implies this deliverable is a decision + plan, not an edit.

## Requirement-to-Evidence Outline

| # | Requirement | Evidence file | Location |
|---|---|---|---|
| R1 | v1 contract `{ theme }` is current, released | `README.md` | L3-7 |
| R2 | v2 target shape `{ appearance: { theme } }` | `README.md` | L9-12 |
| R3 | Reader consumes v1 flat shape today | `src/config.mjs` | L1-3 |
| R4 | No compat/migration/rollout/rollback accepted | `README.md` | L14 |
| R5 | Change must occur in `readTheme` (v1 path) | `src/config.mjs` | L2 |

## Completion Status

**Not implemented — blocked by missing decisions (items 1-4 above), not by code.** No files were modified, per instruction. The correct next step is a written migration decision (compat, migration, rollout, rollback) *before* any source change; once decisions are accepted, the concrete change is a `readTheme` update in `src/config.mjs:1-3` plus whatever migration/rollout mechanism the decisions dictate.

OPENCODE_EXIT=0

--- mutation check ---
