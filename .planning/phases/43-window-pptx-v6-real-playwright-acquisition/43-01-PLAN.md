# Phase 43 Plan

1. Add red tests for cookie parsing, accepted and rejected login state,
   categories, pagination, download, dedupe, resume, and secret redaction.
2. Implement the source-specific Playwright adapter and wire it into the
   dry-run-first library manager.
3. Run focused and related acquisition/private-guard tests.
4. Run the real adapter against the private credential when present; otherwise
   record `NEEDS_AUTH` without weakening the gate.
5. Obtain fresh independent specification and quality reviews.
6. Close only after real private artifacts exist and their hashes/state can be
   spot-checked.
