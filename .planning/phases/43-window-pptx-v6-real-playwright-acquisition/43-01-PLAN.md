# Phase 43: Real Playwright Acquisition - Plan 01

**Created:** 2026-07-30
**Status:** Approved
**Requirements:** V6R-ACQ-01
**Requests:** USR-V6-11, USR-V6-12

## Objective

Replace the incomplete numeric-only adapter with a secure, route-aware,
preview-first, deterministic and resumable real acquisition slice that proves
V6R-ACQ-01 on fixture and authenticated evidence.

## Non-Goals

- Semantic page mining, art-direction certification, or template
  materialization.
- Credential export, access-control bypass, or commercial-byte redistribution.

## Tasks

### Task 1: Route-aware authenticated inventory and bounded acquisition

**Requirements:** V6R-ACQ-01
**Depends on:** Phase 42
**Slice:** Boundary
**Allowed files:** Phase 43 planning, Gaojie acquisition/diversity modules,
library CLI wiring, and acquisition/private-guard tests.
**Forbidden files:** ignored private credentials, packages, previews, and
evidence.

1. Preserve the approved Cookie, origin, redaction, resume, file-cap, and
   atomic-promotion foundation.
2. Replace numeric-only taxonomy discovery with the 32 route-aware
   `products.aspx` categories proven by the authenticated site probe.
3. Inventory paginated `products_show.aspx` cards and their preview images.
4. Add deterministic preview validation, perceptual/color/structure features,
   exact/near-duplicate suppression, and per-category farthest-first selection.
5. Make bounded smoke mode stop after a proven candidate instead of crawling
   all categories and pages.
6. Restrict downloads to direct file suffixes and validate PowerPoint package
   signatures before promotion.
7. Run fixture, full acquisition, private-guard, one-category live, bounded
   live, resume, and diversity-baseline checks.
8. Obtain fresh independent specification, quality, verification, and Phase 43
   audits. Close only after real private artifacts and a complete 32-category
   inventory exist.

**Acceptance:** GOAL-43-01 through GOAL-43-04 all PASS with no unresolved
Blocker or Important.
**Integration points:** library manager source adapter, Playwright browser
context, pinned asset request path, private state, diversity selector.
**Checkpoint:** `43-VALIDATION.md`, `43-COMPLETION-AUDIT.md`, and
`43-SUMMARY.md`.

## Verification

```bash
python -m py_compile skills/owned/window-pptx/scripts/window_pptx/gaojie_playwright.py skills/owned/window-pptx/scripts/window_pptx/gaojie_diversity.py
python -m pytest -q tests/window_pptx/test_acquisition_catalog.py -k 'gaojie_'
python -m pytest -q tests/window_pptx/test_acquisition_catalog.py tests/window_pptx/test_private_asset_guard.py -k 'not gaojie_'
```

## Rollback And Recovery

- Revert tracked adapter changes without deleting ignored private evidence.
- Resume reconciles valid artifacts; missing or corrupt candidates are repaired
  on the next authenticated run.
- Never reset or reuse numeric-only v1 taxonomy completeness.
