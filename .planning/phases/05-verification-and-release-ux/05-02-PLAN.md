---
phase: 05-verification-and-release-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/bootstrap/run-bootstrap.ts
  - src/cli/index.ts
  - README.md
  - tests/bootstrap/bootstrap-smoke.test.ts
  - tests/bootstrap/bootstrap-engine.test.ts
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
user_setup: []
must_haves:
  truths:
    - "Users can see success, failure, and skip status clearly in the tool's final UX"
    - "README documents the finished clone-bootstrap-list-doctor operator flow"
    - "The compiled CLI remains healthy after the expanded command surface lands"
  artifacts:
    - path: "README.md"
      provides: "Final operator-facing usage documentation"
      min_lines: 180
    - path: "tests/bootstrap/bootstrap-smoke.test.ts"
      provides: "Compiled CLI verification coverage"
      min_lines: 120
  key_links:
    - from: "README.md"
      to: "src/cli/index.ts"
      via: "documented final command surface"
      pattern: "list|inspect|doctor"
    - from: "tests/bootstrap/bootstrap-smoke.test.ts"
      to: "dist/cli/index.js"
      via: "compiled verification flow"
      pattern: "doctor|list|inspect"
---

<objective>
Polish the operator-facing verification workflow and documentation.

Purpose: Finish the milestone with a clear end-to-end user story for bootstrap, listing, inspection, and doctor validation.
Output: refined reporting, updated README, and compiled CLI coverage for the final verification flow.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verification-and-release-ux/05-CONTEXT.md
@.planning/phases/05-verification-and-release-ux/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine final reporting for the release UX</name>
  <files>src/bootstrap/run-bootstrap.ts, src/cli/index.ts</files>
  <action>Make sure the final command outputs surface success, skip, and mismatch states clearly enough for users to trust the workflow after bootstrap.</action>
  <verify>npm test -- bootstrap</verify>
  <done>The release UX exposes actionable status instead of raw internal data only.</done>
</task>

<task type="auto">
  <name>Task 2: Update README for the finished operator flow</name>
  <files>README.md</files>
  <action>Update the README so it matches the final command surface and documents how to bootstrap, list, inspect, and doctor a fresh machine setup.</action>
  <verify>npm test -- bootstrap-smoke</verify>
  <done>The README reflects the actual shipped workflow instead of an earlier intermediate phase.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the compiled CLI path green</name>
  <files>tests/bootstrap/bootstrap-smoke.test.ts, tests/bootstrap/bootstrap-engine.test.ts</files>
  <action>Extend verification so the compiled CLI path still works after the new command surface and final reporting changes.</action>
  <verify>npm test -- bootstrap-smoke</verify>
  <done>The published package path remains verified end to end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- bootstrap` passes
- [ ] `npm test -- bootstrap-smoke` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
</verification>

<output>
After completion, create `.planning/phases/05-verification-and-release-ux/05-02-SUMMARY.md`
</output>
