---
phase: 05-verification-and-release-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bootstrap/command-types.ts
  - src/cli/parse-cli.ts
  - src/cli/index.ts
  - tests/cli/bootstrap-command.test.ts
autonomous: true
requirements:
  - VER-01
  - VER-03
user_setup: []
must_haves:
  truths:
    - "The CLI exposes list, inspect, and doctor commands in addition to bootstrap"
    - "Users can inspect target-level installed assets without re-running bootstrap"
    - "Doctor can return machine-readable health results for the current profile"
  artifacts:
    - path: "src/cli/parse-cli.ts"
      provides: "Multi-command parsing"
      min_lines: 70
    - path: "src/cli/index.ts"
      provides: "List/inspect/doctor rendering"
      min_lines: 140
    - path: "tests/cli/bootstrap-command.test.ts"
      provides: "Command-surface verification"
      min_lines: 80
  key_links:
    - from: "src/cli/parse-cli.ts"
      to: "src/cli/index.ts"
      via: "multi-command execution"
      pattern: "list|inspect|doctor"
---

<objective>
Add operator-facing verification commands to the CLI.

Purpose: Make the tool able to answer "what is installed?" and "is it healthy?" without forcing the user to inspect the filesystem manually.
Output: list, inspect, and doctor commands with CLI parsing, rendering, and command-level tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verification-and-release-ux/05-CONTEXT.md
@.planning/phases/05-verification-and-release-ux/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the CLI command surface</name>
  <files>src/bootstrap/command-types.ts, src/cli/parse-cli.ts</files>
  <action>Add explicit `list`, `inspect`, and `doctor` commands with shared flag handling and predictable argument validation.</action>
  <verify>npm test -- cli</verify>
  <done>The binary accepts the new operator-facing verification commands cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Render list, inspect, and doctor output</name>
  <files>src/cli/index.ts</files>
  <action>Wire the new commands into CLI execution and provide both human-readable and JSON output where appropriate.</action>
  <verify>npm test -- cli</verify>
  <done>The CLI can render useful verification output instead of only bootstrap reports.</done>
</task>

<task type="auto">
  <name>Task 3: Add command-surface coverage</name>
  <files>tests/cli/bootstrap-command.test.ts</files>
  <action>Expand the existing CLI tests to cover parsing, help output, and representative responses for the new verification commands.</action>
  <verify>npm test -- cli</verify>
  <done>Phase 5 command UX is guarded by executable tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- cli` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/05-verification-and-release-ux/05-01-SUMMARY.md`
</output>
