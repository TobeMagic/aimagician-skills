---
phase: 28-window-pptx-weak-model-benchmark
plan: 01
type: execute
wave: 1
depends_on:
  - 27.2-window-pptx-portable-backend-cross-engine-qa
files_modified:
  - skills/owned/window-pptx/benchmarks/v5/scenarios.json
  - skills/owned/window-pptx/benchmarks/v5/protocol.json
  - skills/owned/window-pptx/schemas/benchmark-manifest.v1.schema.json
  - skills/owned/window-pptx/schemas/benchmark-scorecard.v1.schema.json
  - skills/owned/window-pptx/schemas/blind-review.v1.schema.json
  - skills/owned/window-pptx/scripts/window_pptx/benchmark.py
  - tests/window_pptx/test_weak_model_benchmark.py
autonomous: true
status: in_progress
requirements:
  - V5-BENCH-01
  - V5-BENCH-02
  - V5-BENCH-03
  - V5-BENCH-04
  - V5-BENCH-05
---

# Phase 28 Plan 01: Frozen Weak-Model Benchmark

**Status:** In Progress — implementation and diagnostics verified; formal execution incomplete
**Requirements:** V5-BENCH-01, V5-BENCH-02, V5-BENCH-03, V5-BENCH-04, V5-BENCH-05

> **Rebaseline note (2026-07-21):** The existing harness, corpus, schemas, manifests, and partial provider evidence are preserved, but no pre-27.2 fingerprint may contribute to the formal aggregate. This plan resumes only from a clean post-27.2 component fingerprint.

## Objective

Build and execute a reproducible benchmark that distinguishes model ability from Skill support across fifteen business scenarios, three arms, two ordinary models, and repeated trials.

## Tasks

- [x] Add failing tests and strict schemas for scenarios, protocol, trials, hashes, deterministic scores, blind reviews, and aggregate verdicts.
- [x] Freeze fifteen synthetic commercial briefs with fact IDs, prohibited inventions, narrative beats, expected archetypes/forms, capacity ranges, and asset conditions.
- [x] Freeze three arms, two ordinary OpenCode model IDs, two repeats, deterministic seeds, prompt contracts, and release thresholds.
- [x] Generate the complete 180-trial manifest with canonical IDs and hashes.
- [x] Implement response ingestion that accepts real provider output only, records unavailable/invalid states, validates DeckPlans, and preserves raw evidence.
- [x] Score fact retention, prohibited invention, compilation, density/rhythm, diversity, editability, QA hard gates, artifacts, and repeat agreement.
- [x] Generate deterministic anonymized blind-review packets and validate the score-sheet schema without exposing arm/model identity; completed human reviews remain open.
- [x] Produce deterministic aggregate arm/model/scenario scorecards, before/after deltas, confidence/completeness evidence, and a fail-closed threshold verdict; the formal aggregate remains open until all evidence exists.
- [ ] Execute available ordinary-model trials through OpenCode, without substituting synthetic outputs when provider calls fail.
- [ ] Generate real governed PptxGenJS PPTX, OOXML reports, LibreOffice PDF/PNG, contact sheets, and Quality-v2 evidence for every available full-v5 trial; retain fake-COM only for legacy-adapter regression.
- [ ] Freeze the Phase 29 PowerPoint certification sample at 10% plus every high-risk capability sample without allowing the sample result to rewrite canonical portable artifacts.
- [ ] Run focused and complete suites, verify all hashes, independently audit conclusions, and document limitations.

## Verification

- Frozen corpus/protocol/schema tests and deterministic 180-trial manifest checks pass.
- Provider fixtures and real diagnostic calls preserve exact raw output and distinct unavailable/invalid/failed/evaluated states.
- Evaluated full-v5 trials contain verified native PPTX, PDF, all page PNGs, contact sheet, OOXML/LibreOffice/Quality-v2 reports, repair log, manifests, and hashes without COM fallback.
- Diagnostic dirty/partial runs are explicitly noncanonical and cannot satisfy formal completeness or release thresholds.
- Formal aggregation rejects missing model/repeat/scenario/review/fingerprint/artifact evidence rather than imputing it.
- `pytest -q tests/window_pptx`, root Vitest, build/typecheck/format, workflow trace, and independent audit pass at the final clean fingerprint.

## Execution Update (2026-07-21)

- Implemented the real portable full-v5 artifact path and fail-closed diagnostic/formal separation.
- Added an immutable formal run contract bound to the 180-trial manifest and clean component fingerprint; formal mode rejects partial selection, replay/manifest-only input, diagnostic imports, and non-exact resume.
- Preserved real DeepSeek responses and regenerated latest-source visual-floor diagnostic artifacts for business-report r12, product-launch r8, and data-analysis r9.
- Added source-grounded relative metric labels, same-family text-capacity repair, compact label cards and single-event milestones, plain-statement/quote separation, narrow governed accent/action rails, and native percentage-axis/label verification without changing the preserved model responses.
- The three latest automatic composites are 98.75, 98.75, and 99.00, with portable hard-gate and native-editability passes. These are diagnostic implementation results only.
- Independently recomputed all three root/trial inventories: 212 SHA-256/size entries, 0 mismatches.
- Phase exit remains blocked by the unexecuted clean two-model 180-trial run, Nemotron/second-model evidence, blind human review, and Phase 29 PowerPoint sample.

## Rollback And Recovery

- Never overwrite an earlier provider or benchmark evidence root; resume only hash-verified completed trials.
- If portable rendering fails, preserve the prompt/provider/response/plan evidence, mark the trial failed, and do not promote or fabricate PPTX artifacts.
- If provider access fails, record unavailable/timeout and continue only the selected diagnostic matrix; do not substitute another model identity.
- If source, protocol, prompt, schema, or fingerprint changes, start a new evidence root and mark the prior run noncanonical.
- If blind-review identity leaks, discard and regenerate only the anonymized packet from verified source artifacts; never rewrite model output.

## Exit

Phase 29 remains blocked if the scenario corpus or protocol can drift silently, model outputs are fabricated or post-edited, arms receive unequal source facts, trial hashes are incomplete, blind review leaks identity, score aggregation imputes missing trials, thresholds are weakened after results, or before/after claims lack recorded evidence.
