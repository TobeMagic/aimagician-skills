---
phase: 02-bootstrap-engine
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/shared/platform.ts
  - src/bootstrap/workspace.ts
  - src/bootstrap/manifest.ts
  - src/bootstrap/plan-bootstrap.ts
  - src/bootstrap/run-bootstrap.ts
  - src/cli/index.ts
  - tests/bootstrap/bootstrap-engine.test.ts
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - TARG-05
user_setup: []
must_haves:
  truths:
    - "Bootstrap creates or updates one user-level workspace consistently on Windows and Linux"
    - "Re-running bootstrap rewrites manifest-backed state deterministically instead of duplicating records"
    - "The engine uses the Phase 1 catalog and normalization pipeline directly to build target-aware install plans"
  artifacts:
    - path: "src/shared/platform.ts"
      provides: "Cross-platform user path resolution"
      min_lines: 30
    - path: "src/bootstrap/run-bootstrap.ts"
      provides: "Bootstrap engine entrypoint"
      min_lines: 40
    - path: "src/bootstrap/manifest.ts"
      provides: "Manifest-backed update logic"
      min_lines: 30
    - path: "tests/bootstrap/bootstrap-engine.test.ts"
      provides: "Bootstrap engine coverage"
      min_lines: 40
  key_links:
    - from: "src/bootstrap/run-bootstrap.ts"
      to: "src/catalog/load-catalog.ts"
      via: "catalog load"
      pattern: "loadCatalog"
    - from: "src/bootstrap/run-bootstrap.ts"
      to: "src/catalog/normalize.ts"
      via: "normalized install planning"
      pattern: "normalizeSources"
    - from: "tests/bootstrap/bootstrap-engine.test.ts"
      to: "src/bootstrap/run-bootstrap.ts"
      via: "bootstrap execution coverage"
      pattern: "runBootstrap"
---

<objective>
Implement the reusable bootstrap engine and user-level workspace layer.

Purpose: Turn the CLI shell into a real bootstrap run that stages normalized assets and maintains idempotent state.
Output: Cross-platform user path helpers, manifest-backed workspace writes, bootstrap planning, and engine tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bootstrap-engine/02-CONTEXT.md
@.planning/phases/02-bootstrap-engine/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-platform user workspace path helpers</name>
  <files>src/shared/platform.ts, src/bootstrap/workspace.ts</files>
  <action>Create a shared abstraction for resolving a user-level bootstrap workspace on Windows and Linux. Keep this separate from repository roots so later target adapters can rely on a stable user-state location.</action>
  <verify>npm run typecheck</verify>
  <done>The engine has one typed place to resolve user-level workspace paths across supported operating systems.</done>
</task>

<task type="auto">
  <name>Task 2: Implement manifest-backed bootstrap planning and apply flow</name>
  <files>src/bootstrap/manifest.ts, src/bootstrap/plan-bootstrap.ts, src/bootstrap/run-bootstrap.ts, src/cli/index.ts</files>
  <action>Load the catalog, normalize assets, apply target filtering, stage a deterministic workspace snapshot, and write a manifest describing the latest bootstrap run. Re-running the command should replace prior state instead of duplicating it.</action>
  <verify>npm test -- bootstrap-engine</verify>
  <done>The package has a real bootstrap engine with deterministic rerun behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add engine tests for workspace writes and idempotence</name>
  <files>tests/bootstrap/bootstrap-engine.test.ts</files>
  <action>Add tests covering cross-platform path resolution, manifest updates, selected-target filtering, and repeat bootstrap runs against the same temp workspace.</action>
  <verify>npm test -- bootstrap-engine</verify>
  <done>The bootstrap engine is covered by executable tests before packaging smoke work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm test -- bootstrap-engine` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/02-bootstrap-engine/02-02-SUMMARY.md`
</output>
