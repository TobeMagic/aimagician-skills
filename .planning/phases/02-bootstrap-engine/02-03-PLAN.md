---
phase: 02-bootstrap-engine
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - package.json
  - README.md
  - tests/bootstrap/bootstrap-smoke.test.ts
autonomous: true
requirements:
  - INST-01
  - INST-05
user_setup: []
must_haves:
  truths:
    - "The package is prepared for npm-executed bootstrap usage"
    - "A built CLI can run a bootstrap smoke flow in an isolated workspace"
    - "Packaging metadata includes the files needed for bootstrap execution"
  artifacts:
    - path: "package.json"
      provides: "npm execution metadata"
      contains: "\"bin\""
    - path: "README.md"
      provides: "Bootstrap usage documentation"
      min_lines: 20
    - path: "tests/bootstrap/bootstrap-smoke.test.ts"
      provides: "Built CLI smoke coverage"
      min_lines: 30
  key_links:
    - from: "tests/bootstrap/bootstrap-smoke.test.ts"
      to: "package.json"
      via: "packaging and build smoke coverage"
      pattern: "npm|pack|build"
---

<objective>
Package the bootstrap engine for npm-style execution and prove it with smoke coverage.

Purpose: Make the repository feel like a real `npx`-friendly bootstrap package before direct target adapters arrive.
Output: Refined package metadata, bootstrap usage docs, and a built-CLI smoke test.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bootstrap-engine/02-CONTEXT.md
@.planning/phases/02-bootstrap-engine/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine package metadata for npm-executed bootstrap usage</name>
  <files>package.json</files>
  <action>Update package metadata so the built CLI is ready for npm execution, distribution, and smoke validation. Keep the package scoped to the current repository reality; the goal is credible packaging, not a final publish step.</action>
  <verify>npm run build</verify>
  <done>The package metadata is aligned with an npm-executed bootstrap workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Document bootstrap usage and state expectations</name>
  <files>README.md</files>
  <action>Add concise repository docs covering install, bootstrap, target selection, and the user-level workspace behavior introduced in this phase. Keep documentation aligned with what is actually implemented now.</action>
  <verify>npm test -- bootstrap-smoke</verify>
  <done>The repository has enough documentation to explain the bootstrap command and its current scope.</done>
</task>

<task type="auto">
  <name>Task 3: Add built-CLI bootstrap smoke coverage</name>
  <files>tests/bootstrap/bootstrap-smoke.test.ts</files>
  <action>Add a smoke test that builds the CLI, runs a bootstrap command in an isolated workspace, and proves the package can execute end to end in a packaging-friendly shape.</action>
  <verify>npm test -- bootstrap-smoke</verify>
  <done>The built package has executable smoke coverage for bootstrap flow and packaging assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `npm test -- bootstrap-smoke` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/02-bootstrap-engine/02-03-SUMMARY.md`
</output>
