---
phase: 02-bootstrap-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/index.ts
  - src/cli/parse-cli.ts
  - src/bootstrap/command-types.ts
  - tests/cli/bootstrap-command.test.ts
autonomous: true
requirements:
  - INST-01
  - INST-05
  - TARG-05
user_setup: []
must_haves:
  truths:
    - "The repository exposes a real bootstrap command instead of a scaffold placeholder"
    - "Target selection defaults to all supported CLIs and can be narrowed from the CLI"
    - "The CLI can render a dry-run style bootstrap result without needing target-specific adapters yet"
  artifacts:
    - path: "src/cli/index.ts"
      provides: "Bootstrap command entrypoint"
      min_lines: 40
    - path: "src/cli/parse-cli.ts"
      provides: "Typed CLI argument parsing"
      min_lines: 30
    - path: "tests/cli/bootstrap-command.test.ts"
      provides: "CLI command coverage"
      min_lines: 30
  key_links:
    - from: "src/cli/index.ts"
      to: "src/cli/parse-cli.ts"
      via: "bootstrap command argument parsing"
      pattern: "parseCli"
    - from: "tests/cli/bootstrap-command.test.ts"
      to: "src/cli/index.ts"
      via: "command execution coverage"
      pattern: "runCli|main"
---

<objective>
Build the bootstrap command surface for the package.

Purpose: Replace the Phase 1 placeholder CLI with a real bootstrap-oriented command model that can become the long-term entrypoint for clone-and-run setup.
Output: Parsed bootstrap flags, readable command output, and tests for default target behavior and overrides.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bootstrap-engine/02-CONTEXT.md
@.planning/phases/02-bootstrap-engine/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the scaffold CLI with a typed bootstrap command parser</name>
  <files>src/cli/index.ts, src/cli/parse-cli.ts, src/bootstrap/command-types.ts</files>
  <action>Implement a typed parser that recognizes `bootstrap`, target selection flags, and dry-run style output options. Keep the parser dependency-light and make `bootstrap` the default command when no explicit subcommand is provided.</action>
  <verify>npm run typecheck</verify>
  <done>The package exposes a real bootstrap command surface with typed argument parsing.</done>
</task>

<task type="auto">
  <name>Task 2: Add readable command UX for bootstrap planning</name>
  <files>src/cli/index.ts</files>
  <action>Render a readable summary that shows selected targets, execution mode, and the next engine step. Do not wire target-specific installs yet; this task only needs a credible operator UX around the bootstrap command.</action>
  <verify>npm test -- bootstrap-command</verify>
  <done>The CLI output is usable as a bootstrap command shell for later engine integration.</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI command tests for defaults and target overrides</name>
  <files>tests/cli/bootstrap-command.test.ts</files>
  <action>Add tests proving `bootstrap` is the effective default command, all supported targets are selected by default, and explicit target overrides are parsed deterministically.</action>
  <verify>npm test -- bootstrap-command</verify>
  <done>The CLI command surface is regression-tested before engine behavior lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm test -- bootstrap-command` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/02-bootstrap-engine/02-01-SUMMARY.md`
</output>
