# Phase 27.1 Validation

**Status:** Implementation verified locally; external evidence incomplete

> **Supersession note (2026-07-21):** These results are historical pre-27.2 evidence. Real portable PPTX/PDF/PNG proof is now owned by Phase 27.2, ordinary-model comparison by Phase 28, and sampled PowerPoint proof by Phase 29. No historical status below has been upgraded without fresh evidence.

| Requirement | Status | Evidence |
|---|---|---|
| V5-REF-01 | PASS | Source commit/archive/subtree/LICENSE hashes, clean-room accept/adapt/reject record, MIT notice, WIP manifest, and 106-file restore drill |
| V5-REF-02 | PASS | Strict schemas and `weak_model.py` authority chain; CLI/facade integration; required-fact, conflict, invention, normalization, and compatibility tests |
| V5-REF-03 | PARTIAL | Twelve profiles, three-candidate selection, six-color BrandSpec/font/pattern gates, passive SVG support, geometry signatures, semantic-form authority, and Recording-COM proofs pass; real PowerPoint proof is `NOT_RUN` |
| V5-REF-04 | PASS | Quality v2 namespaces, pre-save PNG hard gates, canonical protected-content invariants, one pre-render plus one post-render repair, save/reopen semantic validation, monotonic acceptance, and rollback tests pass |
| V5-REF-05 | PARTIAL | Two retries plus fact-safe default, progressive references, eval metadata, misuse tests, and Skillbird checks pass; ordinary-model provider comparison is `NOT_RUN` |
| V5-REF-06 | PARTIAL | Six deterministic post-Huashu calibration packets and a re-frozen 180-trial contract exist; typed component fingerprints and hash-staged PPTX/PNG blind-review gates fail closed; formal clean run is `NOT_RUN` |

## Verification Results

- `pytest -q tests/window_pptx`: **505 passed in 274.44s**.
- `node dist/cli/index.js format-skills --check`: **23 checked, no issues**.
- `python -m compileall -q skills/owned/window-pptx/scripts`: **PASS**.
- `git diff --check`: **PASS**.
- Calibration: six scenarios, two deterministic same-input arms per scenario, 8-10 slides, 5-7 layout families, three enhanced directions, fact coverage `1.0`, native-editable Recording-COM coverage `1.0`, and quality-v2 hard gates pass. Product launch records two safe asset fallbacks. This is calibration-only because the worktree fingerprint is dirty.
- Calibration manifest SHA-256 is `67de551cb7f1a9291928634deec4325daf3558788ddcb2f447aa0d6d1433e9b5`; every listed artifact hash matches, and its current engine/Skill/schema hashes recompute exactly (`engine=e86fafe84785d9b4695e5089ac3e12aa31f43dc7de252c129d37cc9ab521cac9`). Compatibility arms contain no null direction artifact; enhanced arms contain six valid direction decisions.
- Benchmark contract: 180 unique planned trials (15 scenarios × 3 arms × 2 models × 2 repeats); `full-v5` accepts only strict whole-response BriefPlan JSON, emits governed intermediate evidence, verifies provider/response/artifact hashes and thresholds, and requires a clean matching post-Huashu fingerprint bundle before `pending-human-review`. Opaque component hashes, all-invalid runs, and blind scores without verified editable PPTX/PNG evidence are rejected.
- Manifest-only benchmark SHA-256 is `bcfa18da9cfd910dc4391a7b03b8cd7edfa92f7fe222f4ccdb0072f8c96dab9c`; it contains 180 unique IDs, 60 trials per arm, 90 per frozen model, and 90 per repeat index. `fingerprint_artifact` remains null because this was not a formal clean run.

## Explicit Evidence Gaps

- Real PowerPoint PPTX/PDF/PNG generation: `NOT_RUN` for this validation run. The original run saw early-bound `TYPE_E_CANTLOADLIBRARY` (`0x80029C4A`). Later diagnosis proved Microsoft `MSPPT.OLB` and late-bound automation work, while the `_Application` interface registry points to a stale missing WPS TypeLib. No process was force-killed because it could belong to the user.
- OpenCode audit: `NOT_RUN`. `opencode/deepseek-v4-flash-free` session `ses_07fe3a7efffele7rIVoZFjoSEW` stalled after a provider rate limit; the one permitted fallback, `opencode/nemotron-3-ultra-free` session `ses_07fde1154ffeLkYzlM9ln0mFcj`, failed its stream. No verdict was imputed.
- Later research update: DeepSeek read-only session `ses_07d23c7d0ffetnEwNzbfy0zA2b` completed and was independently checked; some inaccurate findings were rejected. Because it predates the final Phase 27.2 implementation, it does not satisfy final gate `V5-UAT-04` or the two-model benchmark.
- Formal ordinary-model benchmark and blind human review: `NOT_RUN`; only the frozen manifest and local deterministic evaluator are verified.

Phase 27.1 remains partially evidenced. Its unresolved portable proof, ordinary-model comparison, and sampled PowerPoint certification are explicitly carried into Phases 27.2, 28, and 29 rather than imputed as Phase 27.1 success.
