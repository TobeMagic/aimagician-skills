# Phase 27.1 User Acceptance

**Status:** NOT_RUN

> **Supersession note (2026-07-21):** This remains an honest record that the Phase 27.1 COM-based UAT was not run. It no longer blocks governed daily delivery: Phase 27.2 owns real portable UAT, while Phase 29 owns sampled read-only PowerPoint certification.

## Intended Matrix

- Generate the six calibration cases as editable PPTX.
- Reopen each candidate and verify native text, chart, table, diagram, and image editability.
- Export PDF and page-ratio-correct PNG.
- Inspect contact sheets for hierarchy, crop, whitespace, repetition, font fallback, chart labels, and brand consistency.
- Confirm source integrity, package validity, and absence of PowerPoint process residue.

## Blocking Evidence

The original Phase 27.1 early-bound COM run failed with `TYPE_E_CANTLOADLIBRARY` (`0x80029C4A`). Later diagnosis showed that Microsoft `MSPPT.OLB` loads and late-bound PowerPoint works, but `_Application` is registered to a stale missing WPS TypeLib. No real candidate PPTX/PDF/PNG was produced during this Phase 27.1 UAT, and Recording COM evidence is not treated as UAT.

The production transaction now performs a second semantic/editability inspection on the reopened candidate before atomic promotion, but this path still requires a functioning real PowerPoint host before it counts as UAT evidence.

No broad `POWERPNT.EXE` termination was attempted because an existing process may belong to the user.
