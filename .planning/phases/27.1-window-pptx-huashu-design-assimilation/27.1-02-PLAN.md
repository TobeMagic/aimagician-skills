---
phase: 27.1-window-pptx-huashu-design-assimilation
plan: 02
type: execute
wave: 2
status: Accepted
execution_status: In Progress
requirements: [V5-REF-03]
---

# Plan 02: Native Art Direction and Design System

> **Supersession note (2026-07-21):** Recording-COM and the historical COM proof route remain preserved. The missing real-artifact proof is now implemented and accepted under Phase 27.2's portable renderer and cross-engine gates; sampled PowerPoint proof moves to Phase 29.

**Requirements:** V5-REF-03
**Status:** Accepted

## Objective

Implement a deterministic native-PPTX art-direction, brand, layout, theme, and asset system that raises the design floor without model-authored styling.

## Tasks

- [x] Add twelve independently authored art-direction profiles and deterministic three-candidate selection.
- [x] Add strict BrandSpec, font inventory digest, safe asset requirements, and direction decision traces.
- [x] Add geometry signatures and weighted layout selection with fail-closed asset/capacity filtering.
- [x] Make selected semantic form authoritative for native chart/diagram generation.
- [x] Activate governed typography, color, spacing, radius, border, shadow, decoration, chart, and table tokens where COM supports them.
- [ ] Generate native self-owned proof/showcase fixtures and visual regression evidence.

Recording-COM proof plans and deterministic calibration packets exist. Real PowerPoint PPTX/PDF/PNG proof remains `NOT_RUN` because the local PowerPoint type library fails with `TYPE_E_CANTLOADLIBRARY` (`0x80029C4A`).

## Verification

- Registry and schema loaders reject drift and unknown fields.
- Direction and layout selection are deterministic and structurally distinct across page ratios.
- Recording fake-COM and Windows proof routes preserve native object and transaction gates.

## Rollback And Recovery

Keep `legacy-v5` as an explicit trusted design-system version for replay. Never silently change old geometry IDs; use new registry versions and aliases.
