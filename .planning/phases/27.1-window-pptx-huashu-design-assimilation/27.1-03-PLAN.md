---
phase: 27.1-window-pptx-huashu-design-assimilation
plan: 03
type: execute
wave: 3
status: Accepted
execution_status: In Progress
requirements: [V5-REF-04, V5-REF-05, V5-REF-06]
---

# Plan 03: Quality v2, Skill TDD, and Calibration

> **Supersession note (2026-07-21):** The historical provider failures remain recorded. A later DeepSeek audit is research evidence only; Phase 28 owns the two-model benchmark and Phase 29 owns the final post-implementation audit.

**Requirements:** V5-REF-04, V5-REF-05, V5-REF-06
**Status:** Accepted

## Objective

Complete cross-stage quality evidence, bounded repair, progressive Skill guidance, calibration, fingerprints, and independent review.

## Tasks

- [x] Merge narrative/compiler/render/preview/package/editability findings into quality-report v2.
- [x] Implement one pre-render and one post-render monotonic repair with immutable-fact invariants and rollback.
- [x] Add PNG-based crop, duplicate, whitespace, hierarchy, anti-slop, and direction-drift checks.
- [x] Refactor SKILL.md for progressive disclosure and add weak-model, narrative, design, brand, and QA references.
- [x] Freeze six calibration cases and complete same-input compatibility/enhanced evidence.
- [x] Re-freeze the Phase 28 protocol with complete fingerprints, strict thresholds, and identity-bound artifact hashes.
- [ ] Obtain a final bounded OpenCode audit verdict from one of the two frozen ordinary-model providers.

The 180-trial contract is re-frozen and complete aggregation now rejects missing, dirty, placeholder, mixed, pre-Huashu, artifact-mismatched, invalid-response, or threshold-failing evidence. Environment component hashes must bind typed manifests, and blind scoring requires staged hash-verified readable PPTX/PNG evidence. The bounded OpenCode audit was attempted with both frozen ordinary models, but provider rate-limit/stream failures produced no verdict; the formal run remains `NOT_RUN`.

## Verification

- Quality v2 schemas, finding order/dedupe, repair monotonicity, rollback, and edit-sentinel tests pass.
- Skill pressure/misuse evals and `skillbird format-skills --check` pass.
- Calibration and benchmark manifests verify every fingerprint and reject pre-Huashu or mixed runs.

## Rollback And Recovery

Quality v1 readers remain supported. Failed replan/repair restores its snapshot and cannot promote a final deck. Provider and Windows failures remain explicit evidence gaps rather than fabricated PASS results.
