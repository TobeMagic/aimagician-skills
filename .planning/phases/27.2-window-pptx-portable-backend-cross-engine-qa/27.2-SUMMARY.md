# Phase 27.2 Summary

**Status:** Complete

## Delivered Outcome

The governed production path is now:

`FactStore -> BriefPlan -> NarrativePlan -> DeckPlan -> RenderPlan -> PptxGenJS -> deterministic OOXML -> semantic inspection -> LibreOffice/Poppler proof -> Quality-v2 -> atomic promotion`

PowerPoint remains available for explicit legacy capabilities and sampled read-only release certification, not daily delivery.

## Current Verified State

- Backend-neutral capability negotiation, PptxGenJS 4.0.1 rendering, deterministic OOXML normalization, exact RenderPlan-aware semantic inspection, isolated LibreOffice/Poppler proof, Quality-v2, atomic promotion, proof-only HTML, and optional safe PowerPoint certification are implemented.
- The COM root cause is confirmed: stale WPS `_Application` TypeLib registration breaks early binding while Microsoft `MSPPT.OLB` and late-bound PowerPoint remain usable. No automatic registry repair is attempted.
- Six canonical portable packets cover 56 pages, 16:9/4:3/custom ratios, native image/chart/table/diagram/note/link/master/footer evidence, and two byte-identical renders per case. Every portable hard gate passes and the root inventory verifies 200 files.
- The visual floor improved materially: empty decoration frames 14→0, orphan agenda continuations 2→0, generic classifier titles 28→0, big-number/KPI pages 0→14, role-aware masters and title hierarchy added, and charts/tables receive governed styling.
- Manual review still fails all six scenarios against the user's customer-delivery/senior-designer bar. This is recorded in the manifest and is not inferred away from automated PASS.
- Historical and failed/mixed calibration packets remain preserved under separate evidence directories and are not reused as the final packet.
- The final frozen Window-PPTX regression passes 602/602 tests. Python compilation, TypeScript typecheck/build, Skillbird format checking, diff checking, and the clean root Vitest rerun (101/101) pass. Two unrelated bootstrap/acceptance timeouts from an earlier concurrent run passed individually and in the unloaded full rerun.
- OpenCode 1.17.6 session `ses_07c11cc8bffe8HmjcQX73B1We4` with `opencode/deepseek-v4-flash-free` independently confirms `A=PASS` for portable engineering, `B=FAIL` for customer visual quality, `RELEASE=NO_GO`, no remaining planning conflict, 602/602 regression evidence, and present V5-PORT-07 doctor/certification tests.

## Outstanding For The v5 Milestone

- Raise content-to-visual mapping beyond sparse statement pages, add governed asset acquisition/fallbacks, strengthen analytical annotation, training pedagogy, and industry/brand differentiation, then repeat manual review.
- Refreeze Phase 28 from a clean post-27.2 fingerprint and execute available ordinary-model trials plus blind review without imputing provider failures.
- Run Phase 29 ten-run reliability, high-risk capability samples, real read-only PowerPoint certification, and the final independent OpenCode audit.

## Completion Rule

Phase 27.2 is complete because every accepted V5-PORT requirement has fresh portable PASS evidence. Preserve failed and `NOT_RUN` checks explicitly: this summary does not infer PowerPoint compatibility, weak-model superiority, blind-review quality, customer visual acceptance, or senior-designer equivalence from portable tests.
