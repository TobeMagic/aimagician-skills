# Phase 27.2 Validation

**Status:** PASS — portable engineering requirements verified; manual customer visual bar remains a v5 milestone blocker

Historical Phase 27.1 results, including 505 Python tests and Recording-COM calibration, are baseline evidence only. They do not count as Phase 27.2 PASS until rerun against the final portable implementation and fingerprint.

| Requirement | Status | Required Evidence |
|---|---|---|
| V5-PORT-01 | PASS | Backend-selection/capability fixtures prove governed `auto` chooses PptxGenJS with COM denied and unsupported requirements fail before process/filesystem mutation. |
| V5-PORT-02 | PASS | Native editable text, shapes, image/crop, table, chart, diagrams, notes, links, role masters, and footers pass exact RenderPlan-bound OOXML checks across 16:9, 4:3, and custom ratios. |
| V5-PORT-03 | PASS | Every final calibration case has a two-run byte-identical deterministic report; candidate/source hashes and atomic promotion guards pass. |
| V5-PORT-04 | PASS | Positive packages and corrupted content-type, relationship, identity, geometry, alpha/weight/shadow, media, chart, table, note, link, master, and duplicate-ID fixtures fail closed with stable codes. |
| V5-PORT-05 | PASS | Six isolated LibreOffice/Poppler pipelines produce ratio/page-count-correct PDF/PNG, preserve canonical PPTX hashes, feed Quality-v2, and verify cleanup/failure behavior. |
| V5-PORT-06 | PASS | RenderPlan-derived proof DOM is deterministic and proof-only; model-authored HTML and HTML-as-canonical-PPTX paths are absent/rejected. Huashu is method-only with no copied runtime/prompt/media. |
| V5-PORT-07 | PASS | Doctor fixtures identify the stale WPS `_Application` TypeLib registration, preserve registry state, use bounded late-bound ownership, and keep certification optional/read-only/non-saving. Real PowerPoint certification remains intentionally `NOT_RUN`. |
| V5-PORT-08 | PASS | Backend/verification CLI matrices, stable schemas/readers, capability decisions, artifact hashes, and Python/Node/PptxGenJS/LibreOffice/Poppler/font/OS fingerprints pass compatibility tests. |
| V5-PORT-09 | PASS | Final packet has 6/6 real portable scenarios, 56 pages, all required PPTX/PDF/PNG/contact-sheet/reports, all portable hard gates PASS, deterministic bytes, and a verified 200-file root inventory. Manual visual verdict is separately FAIL and not hidden. |

## Required Commands And Inspections

- Focused Python tests for portable backend, OOXML, LibreOffice, HTML proof, certification, reports, and calibration.
- `pytest -q tests/window_pptx`.
- Node worker tests, `npm run typecheck`, `npm test`, and `npm run build` as applicable to the final implementation.
- `python -m compileall -q skills/owned/window-pptx/scripts`.
- `node dist/cli/index.js format-skills --check` after a fresh build.
- Schema loading and canonical hash verification for every staged artifact.
- `git diff --check` and direct inspection of all six PPTX/PDF/PNG/contact-sheet packets.
- `libreoffice --version`, `pdfinfo -v`, `pdftoppm -v`, Node/npm version, font inventory, and OS/locale fingerprints.
- `aimagician-superpower` workflow status/trace/validation where the decimal phase is supported; otherwise record the tool limitation and perform manual requirement-to-plan-to-evidence tracing.

## Planning Gate Evidence — 2026-07-21

- The installed workflow runtime does not resolve the numeric decimal alias `--phase 27.2` and returns `PHASE_NOT_FOUND`. The full slug `--phase 27.2-window-pptx-portable-backend-cross-engine-qa` resolves correctly; this alias limitation must be recorded in handoffs.
- With the full slug, workflow `spec`, `plan`, and `execute` gates pass after all three plan bodies record `Status: Accepted`.
- At the pre-implementation planning checkpoint, full-slug `trace` marked every V5-PORT requirement as planned and returned the expected `TRACE_NO_EVIDENCE` gaps while validation was still `NOT_RUN`; the final trace below supersedes that checkpoint with evidence=PASS.
- Manual trace confirms nine unique accepted Phase 27.2 requirements, nine matching rows in `.planning/REQUIREMENTS.md`, and complete one-time plan coverage: Plan 01 maps PORT-01..03, Plan 02 maps PORT-04..06, and Plan 03 maps PORT-07..09.
- Repository-wide v5 trace contains 59 unique requirement IDs and 59 unique trace rows with no missing or unknown IDs.
- Final full-slug workflow `status` is `complete` and `trace` is `covered`; every V5-PORT requirement resolves to planned=yes/evidence=PASS. The numeric decimal alias limitation remains unchanged.

## Final Evidence — 2026-07-21

- Canonical calibration: `.planning/phase-27.2-evidence/calibration/manifest.json`.
- Human visual decision: `.planning/phase-27.2-evidence/calibration/manual-visual-inspection.json`.
- Six cases: 6 PASS, 0 FAIL; 56 slides; 16:9, 4:3, and custom profiles.
- Per-case gates: deterministic bytes, OOXML semantic parity, LibreOffice PDF, Poppler PNG, Quality-v2, source integrity, and atomic promotion all PASS.
- Root SHA-256 inventory: 200 entries verified after sealing the manual review.
- Post-run source check: current engine, registry, schema, and Skill/reference tree hashes exactly match the frozen manifest (`1ae852…`, `6a1e1b…`, `b8dbec…`, `83cc8a…` respectively); only planning and test records changed after generation.
- Runtime: Python 3.13.12, Node v20.19.2, npm 9.2.0, PptxGenJS 4.0.1, LibreOffice 25.2.3.2, Poppler 26.02.0, WSL2 Linux x86_64, and a non-empty 59-font inventory.
- Focused frozen checks: core + advanced renderer 83 PASS; trusted-metric/process-rotation calibration regression 3 PASS; Python compilation, TypeScript typecheck/build, Skillbird format check, and `git diff --check` PASS.
- Root Vitest clean rerun: 21 files and 101/101 tests PASS in 41.36 seconds. The earlier concurrent run timed out in two unrelated Skillbird acceptance/bootstrap tests; each passed alone (5/5 and 2/2) and the unloaded full rerun closed the timing issue.
- Complete frozen `pytest -q tests/window_pptx`: 602 PASS, 0 FAIL in 840.55 seconds. The earlier concurrent run (598 PASS, 2 stale failures) is explicitly noncanonical because related source/tests changed during execution; both stale failures are covered by the final clean run.
- Final read-only OpenCode 1.17.6 re-audit used `opencode/deepseek-v4-flash-free`, session `ses_07c11cc8bffe8HmjcQX73B1We4`, exit 0. Its raw verdict is `A=PASS; B=FAIL; RELEASE=NO_GO; PLANNING_CONFLICT=NO; REGRESSION_602=CONFIRMED; V5_PORT_07_TEST_EVIDENCE=PRESENT`; direct repository checks agree. This is Phase 27.2 review evidence, not the later Phase 29 final audit gate.
- Formal Phase 28 eligibility is false because the worktree is dirty; deterministic calibration did not invoke a model. Real PowerPoint certification, ordinary-model trials, and blind human comparison remain `NOT_RUN`.
- Visual comparison: unbound decorations 14→0, orphan agenda continuations 2→0, generic classifier headings 28→0, big-number/KPI pages 0→14. All six decks still fail the requested customer-delivery/senior-designer visual bar due to low asset coverage and underdeveloped content-to-visual specialization.

## Completion Rule

Every V5-PORT requirement has fresh portable PASS evidence. Phase 27.2 does not require a real PowerPoint certification sample, but the doctor/adapter safety tests and explicit Phase 29 handoff pass. Closing Phase 27.2 is an engineering conclusion only; v5.0 remains active because manual customer visual acceptance failed and Phases 28-29 are incomplete.
