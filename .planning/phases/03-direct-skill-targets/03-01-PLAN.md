---
phase: 03-direct-skill-targets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/platform.ts
  - src/bootstrap/target-homes.ts
  - src/bootstrap/plan-bootstrap.ts
  - src/bootstrap/run-bootstrap.ts
  - tests/bootstrap/direct-target-sync.test.ts
autonomous: true
requirements:
  - INST-04
  - TARG-01
user_setup: []
must_haves:
  truths:
    - "Bootstrap resolves the current user's Codex skill home deterministically on Windows and Linux"
    - "Codex can receive copied skill directories from the bootstrap engine without writing into repository-local paths"
    - "The bootstrap result can distinguish direct-sync targets from deferred targets"
  artifacts:
    - path: "src/bootstrap/target-homes.ts"
      provides: "Direct target-home resolution"
      min_lines: 30
    - path: "src/bootstrap/run-bootstrap.ts"
      provides: "Bootstrap apply integration for direct targets"
      min_lines: 70
    - path: "tests/bootstrap/direct-target-sync.test.ts"
      provides: "Codex direct sync coverage"
      min_lines: 50
  key_links:
    - from: "src/bootstrap/run-bootstrap.ts"
      to: "src/bootstrap/target-homes.ts"
      via: "target-home resolution"
      pattern: "resolve.*Target"
    - from: "tests/bootstrap/direct-target-sync.test.ts"
      to: "src/bootstrap/run-bootstrap.ts"
      via: "direct sync execution coverage"
      pattern: "runBootstrap"
---

<objective>
Land the direct target-home abstraction and the first concrete adapter flow for Codex.

Purpose: Establish the Phase 3 path boundary and prove that the bootstrap engine can install real skill directories into a current-user target home.
Output: Central target-home resolution, bootstrap integration for direct-target reporting, and Codex-focused direct sync coverage.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-direct-skill-targets/03-CONTEXT.md
@.planning/phases/03-direct-skill-targets/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add centralized direct target-home resolution</name>
  <files>src/shared/platform.ts, src/bootstrap/target-homes.ts</files>
  <action>Extend the shared platform model so bootstrap code can resolve current-user homes for direct skill targets under both Windows and Linux. Keep Codex, Claude Code, and OpenCode paths in one typed resolver instead of scattering literals across the engine.</action>
  <verify>npm run typecheck</verify>
  <done>The project has one reusable resolver for direct skill target homes.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Codex direct sync into bootstrap apply flow</name>
  <files>src/bootstrap/plan-bootstrap.ts, src/bootstrap/run-bootstrap.ts</files>
  <action>Extend bootstrap planning and apply reporting so direct-target work can be tracked separately from workspace state. Wire the first real direct target flow for Codex without breaking dry-run or deferred target handling.</action>
  <verify>npm test -- direct-target-sync</verify>
  <done>Bootstrap can report and execute a Codex direct-target sync path.</done>
</task>

<task type="auto">
  <name>Task 3: Add isolated Codex direct sync coverage</name>
  <files>tests/bootstrap/direct-target-sync.test.ts</files>
  <action>Create fake-home integration coverage proving Codex installs land under the isolated user home and do not depend on the real machine profile.</action>
  <verify>npm test -- direct-target-sync</verify>
  <done>The first direct target path is guarded by executable tests before the remaining adapters land.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm test -- direct-target-sync` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/03-direct-skill-targets/03-01-SUMMARY.md`
</output>
