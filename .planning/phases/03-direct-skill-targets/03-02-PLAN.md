---
phase: 03-direct-skill-targets
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/bootstrap/direct-target-sync.ts
  - src/bootstrap/plan-bootstrap.ts
  - src/bootstrap/run-bootstrap.ts
  - tests/bootstrap/direct-target-sync.test.ts
autonomous: true
requirements:
  - INST-04
  - TARG-02
  - TARG-03
user_setup: []
must_haves:
  truths:
    - "Bootstrap installs configured skills into Claude Code user-level skill locations"
    - "Bootstrap installs configured skills into OpenCode user-level skill locations"
    - "Owned and repository-resolved external skills both flow through the same direct sync mechanism"
  artifacts:
    - path: "src/bootstrap/direct-target-sync.ts"
      provides: "Shared direct skill sync logic"
      min_lines: 70
    - path: "src/bootstrap/run-bootstrap.ts"
      provides: "Claude and OpenCode bootstrap apply integration"
      min_lines: 90
    - path: "tests/bootstrap/direct-target-sync.test.ts"
      provides: "Claude and OpenCode direct sync coverage"
      min_lines: 80
  key_links:
    - from: "src/bootstrap/direct-target-sync.ts"
      to: "src/bootstrap/target-homes.ts"
      via: "per-target sync destinations"
      pattern: "skillsDir|target"
    - from: "src/bootstrap/run-bootstrap.ts"
      to: "src/bootstrap/direct-target-sync.ts"
      via: "direct sync apply"
      pattern: "syncDirect"
---

<objective>
Complete the direct skill-folder adapters for Claude Code and OpenCode.

Purpose: Finish the Phase 3 target set so Codex, Claude Code, and OpenCode all receive managed skill directories from the same bootstrap pipeline.
Output: Shared direct-sync implementation with Claude and OpenCode coverage.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-direct-skill-targets/03-CONTEXT.md
@.planning/phases/03-direct-skill-targets/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared direct skill sync logic for target homes</name>
  <files>src/bootstrap/direct-target-sync.ts, src/bootstrap/plan-bootstrap.ts</files>
  <action>Implement a reusable copy-based direct sync layer that can materialize concrete skill directories for selected targets. Keep the logic target-agnostic once target homes and concrete source directories are known.</action>
  <verify>npm run typecheck</verify>
  <done>The engine can sync the same resolved skill asset into multiple direct targets through one shared implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Claude Code and OpenCode apply behavior</name>
  <files>src/bootstrap/run-bootstrap.ts, src/bootstrap/direct-target-sync.ts</files>
  <action>Extend the bootstrap apply path so Claude Code and OpenCode receive copied skill directories under their current-user homes. Preserve explicit Gemini deferral instead of pretending Phase 4 behavior already exists.</action>
  <verify>npm test -- direct-target-sync</verify>
  <done>Claude Code and OpenCode are first-class direct skill targets in the bootstrap engine.</done>
</task>

<task type="auto">
  <name>Task 3: Expand fake-home integration tests across all direct targets</name>
  <files>tests/bootstrap/direct-target-sync.test.ts</files>
  <action>Add integration tests that prove owned skills and GitHub-resolved external skills land in Codex, Claude Code, and OpenCode homes under isolated fake-user directories.</action>
  <verify>npm test -- direct-target-sync</verify>
  <done>All three direct targets are covered by executable sync tests before manifest prune work lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm test -- direct-target-sync` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/03-direct-skill-targets/03-02-SUMMARY.md`
</output>
