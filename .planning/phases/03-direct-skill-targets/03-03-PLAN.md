---
phase: 03-direct-skill-targets
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/bootstrap/manifest.ts
  - src/bootstrap/run-bootstrap.ts
  - src/cli/index.ts
  - README.md
  - tests/bootstrap/bootstrap-engine.test.ts
  - tests/bootstrap/bootstrap-smoke.test.ts
  - tests/bootstrap/direct-target-sync.test.ts
autonomous: true
requirements:
  - INST-04
  - TARG-01
  - TARG-02
  - TARG-03
user_setup: []
must_haves:
  truths:
    - "Re-running bootstrap updates managed direct-target installs without creating duplicate skill directories"
    - "Managed direct-target skill directories are pruned when they are no longer selected"
    - "The built CLI can execute direct-target bootstrap logic against an isolated fake home"
  artifacts:
    - path: "src/bootstrap/manifest.ts"
      provides: "Manifest fields for direct-target sync state"
      min_lines: 40
    - path: "tests/bootstrap/bootstrap-engine.test.ts"
      provides: "Manifest-backed direct sync regression coverage"
      min_lines: 60
    - path: "tests/bootstrap/bootstrap-smoke.test.ts"
      provides: "Built CLI direct-target smoke coverage"
      min_lines: 40
  key_links:
    - from: "src/bootstrap/run-bootstrap.ts"
      to: "src/bootstrap/manifest.ts"
      via: "managed direct-target install tracking"
      pattern: "manifest"
    - from: "tests/bootstrap/bootstrap-smoke.test.ts"
      to: "src/cli/index.ts"
      via: "compiled CLI bootstrap execution"
      pattern: "dist/cli/index.js"
---

<objective>
Finish Phase 3 with manifest-backed direct-sync idempotence and built-CLI coverage.

Purpose: Make direct skill installs safe to rerun and safe to clean up without touching unrelated user content.
Output: Manifest extensions, stale-managed prune behavior, updated CLI/reporting, and built-CLI smoke validation against isolated fake homes.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-direct-skill-targets/03-CONTEXT.md
@.planning/phases/03-direct-skill-targets/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the manifest for direct-target sync and prune</name>
  <files>src/bootstrap/manifest.ts, src/bootstrap/run-bootstrap.ts, tests/bootstrap/bootstrap-engine.test.ts</files>
  <action>Record enough direct-target state in the manifest to identify managed skill directories from the previous run and safely prune only stale managed installs. Keep manifest comparison deterministic so reruns remain easy to reason about.</action>
  <verify>npm test -- bootstrap-engine</verify>
  <done>The manifest supports safe direct-target reruns and stale-managed prune behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI and docs for direct-target bootstrap output</name>
  <files>src/cli/index.ts, README.md</files>
  <action>Expose direct-target results in the CLI preview/output so users can see which targets were synced and which were deferred. Update repository docs to match the real Phase 3 behavior, including isolated-home overrides for testing.</action>
  <verify>npm run build</verify>
  <done>The operator-facing output and docs match the actual direct-target bootstrap behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add rerun and built-CLI smoke coverage for direct targets</name>
  <files>tests/bootstrap/direct-target-sync.test.ts, tests/bootstrap/bootstrap-smoke.test.ts</files>
  <action>Add tests proving reruns do not duplicate skill directories, stale managed installs are pruned, and the compiled CLI can apply direct-target logic against a fake user home instead of the real machine profile.</action>
  <verify>npm test -- bootstrap-smoke</verify>
  <done>Direct-target bootstrap behavior is covered both in-process and through the built CLI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `npm test -- bootstrap-engine` passes
- [ ] `npm test -- bootstrap-smoke` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/03-direct-skill-targets/03-03-SUMMARY.md`
</output>
