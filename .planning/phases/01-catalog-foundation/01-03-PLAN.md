---
phase: 01-catalog-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/model/assets.ts
  - src/model/targets.ts
  - src/catalog/normalize.ts
  - tests/catalog/target-mapping.test.ts
  - tests/fixtures/catalog/target-overrides.yaml
autonomous: true
requirements:
  - SRC-04
user_setup: []
must_haves:
  truths:
    - "Catalog data can express default all-target behavior plus source-level and asset-level overrides"
    - "Skills and plugins share one target expression style even though they live in different catalog sections"
    - "Unsupported target or capability combinations can be represented for later skip+warn behavior"
  artifacts:
    - path: "src/model/targets.ts"
      provides: "Target and capability typing"
      min_lines: 30
    - path: "src/model/assets.ts"
      provides: "Normalized asset model"
      min_lines: 30
    - path: "src/catalog/normalize.ts"
      provides: "Target override merge logic"
      min_lines: 30
    - path: "tests/catalog/target-mapping.test.ts"
      provides: "Target mapping and override validation"
      min_lines: 30
  key_links:
    - from: "src/catalog/normalize.ts"
      to: "src/model/targets.ts"
      via: "target merge and capability typing"
      pattern: "target|capability|override"
    - from: "tests/catalog/target-mapping.test.ts"
      to: "src/catalog/normalize.ts"
      via: "fixture-backed target override checks"
      pattern: "normalize"
---

<objective>
Implement the normalized asset and target-mapping model for Phase 1.

Purpose: Lock how source records become target-aware internal objects so later installer phases do not need to reinterpret the catalog shape.
Output: Target typing, normalized asset types, override merge logic, and target-mapping tests.
</objective>

<execution_context>
@C:/Users/AImagician/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AImagician/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-foundation/01-CONTEXT.md
@.planning/phases/01-catalog-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define normalized asset and target types</name>
  <files>src/model/assets.ts, src/model/targets.ts</files>
  <action>Create the internal types for assets, targets, and capability metadata. Reflect the locked decisions exactly: global default all-target behavior, source-level overrides, asset-level overrides, and shared target syntax for skills and plugins.</action>
  <verify>npm run typecheck</verify>
  <done>The project has one typed model for target-aware assets that later phases can consume directly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement target normalization and override merging</name>
  <files>src/catalog/normalize.ts</files>
  <action>Implement the logic that turns parsed catalog records into normalized assets with effective target lists and capability metadata. Preserve enough information for later skip+warn behavior, but do not implement installation yet.</action>
  <verify>npm test -- target-mapping</verify>
  <done>Catalog entries can be normalized into a consistent internal representation with deterministic target results.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixture-backed override tests</name>
  <files>tests/catalog/target-mapping.test.ts, tests/fixtures/catalog/target-overrides.yaml</files>
  <action>Create representative fixtures that prove global defaults, source overrides, asset overrides, and shared syntax across skills/plugins work as intended. Include at least one unsupported target or capability example to preserve skip+warn metadata for later phases.</action>
  <verify>npm test</verify>
  <done>The target mapping model is executable, regression-tested, and ready for downstream adapters.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm test -- target-mapping` passes
- [ ] `npm test` passes
</verification>

<success_criteria>

- All tasks completed
- Target declarations can be expressed once and normalized predictably
- Source and asset override behavior is encoded in tests
- Later phases can consume normalized assets without redesigning the catalog
</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-foundation/01-03-SUMMARY.md`
</output>
