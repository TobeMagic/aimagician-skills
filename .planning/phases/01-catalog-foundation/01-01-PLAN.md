---
phase: 01-catalog-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/cli/index.ts
  - src/shared/paths.ts
  - tests/scaffold/scaffold.test.ts
  - tests/fixtures/.gitkeep
  - skills/owned/.gitkeep
  - catalog/skills/.gitkeep
  - catalog/plugins/.gitkeep
autonomous: true
requirements:
  - REPO-01
  - REPO-02
user_setup: []
must_haves:
  truths:
    - "The repository has a working TypeScript and Vitest baseline for future catalog work"
    - "Owned skills have a locked home under skills/owned that later scanners can rely on"
    - "Core repository and test directories exist before catalog logic is added"
  artifacts:
    - path: "package.json"
      provides: "Project scripts and dependency baseline"
      contains: "\"test\""
    - path: "src/shared/paths.ts"
      provides: "Centralized repository path constants"
      min_lines: 20
    - path: "tests/scaffold/scaffold.test.ts"
      provides: "Scaffold smoke coverage"
      min_lines: 10
    - path: "skills/owned/.gitkeep"
      provides: "Locked owned-skills root"
    - path: "catalog/skills/.gitkeep"
      provides: "Skills catalog root"
    - path: "catalog/plugins/.gitkeep"
      provides: "Plugins catalog root"
  key_links:
    - from: "src/cli/index.ts"
      to: "src/shared/paths.ts"
      via: "imports repository path helpers"
      pattern: "from\\s+['\\\"]\\.\\./shared/paths['\\\"]"
---

<objective>
Establish the executable repository baseline for Phase 1.

Purpose: Create the TypeScript/Vitest scaffold and lock the repository roots that later plans will fill with catalog and model logic.
Output: Working project scaffold, centralized path helpers, and canonical owned/catalog directories.
</objective>

<execution_context>
@C:/Users/AImagician/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AImagician/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-foundation/01-CONTEXT.md
@.planning/phases/01-catalog-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the TypeScript and Vitest baseline</name>
  <files>package.json, tsconfig.json, vitest.config.ts, src/cli/index.ts</files>
  <action>Create the initial npm package, TypeScript configuration, Vitest configuration, and a minimal CLI entrypoint. Keep scripts focused on `build`, `typecheck`, and `test` so later plans can add real catalog commands without reshaping the project scaffold.</action>
  <verify>npm test</verify>
  <done>The repository has installable dependencies and working baseline scripts for build, typecheck, and test.</done>
</task>

<task type="auto">
  <name>Task 2: Lock repository roots and path helpers</name>
  <files>src/shared/paths.ts, skills/owned/.gitkeep, catalog/skills/.gitkeep, catalog/plugins/.gitkeep, tests/fixtures/.gitkeep</files>
  <action>Create the canonical directories required by the phase decisions and add a single shared path module that names these roots explicitly. Do not guess future adapter paths here; only encode the repository-level roots decided in CONTEXT.md.</action>
  <verify>npm run typecheck</verify>
  <done>The owned-skills and catalog roots exist, and later plans have one central place to read those paths from.</done>
</task>

<task type="auto">
  <name>Task 3: Add a minimal scaffold smoke test</name>
  <files>tests/scaffold/scaffold.test.ts, vitest.config.ts, package.json</files>
  <action>Add at least one smoke test or fixture-backed test hook so the repository can prove the scaffold works before catalog logic lands. The goal is not feature coverage yet; it is to ensure the test harness is real and ready for Wave 2.</action>
  <verify>npm test</verify>
  <done>Vitest runs successfully in the new repository and fails loudly if the scaffold breaks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` succeeds
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
</verification>

<success_criteria>

- All tasks completed
- The repository is executable as a TypeScript project
- Owned and catalog roots match the Phase 1 decisions
- Wave 2 plans can build on the scaffold without redefining repository structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-foundation/01-01-SUMMARY.md`
</output>
