---
phase: 01-catalog-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/catalog/schemas.ts
  - src/catalog/load-catalog.ts
  - src/catalog/source-types.ts
  - catalog/skills/example.yaml
  - catalog/plugins/example.yaml
  - tests/catalog/source-catalog.test.ts
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
user_setup: []
must_haves:
  truths:
    - "The catalog can represent GitHub and command-based external sources"
    - "An external source can be enabled or disabled without deleting its definition"
    - "Skills and plugins have separate catalog sections while using the same core source vocabulary"
  artifacts:
    - path: "src/catalog/schemas.ts"
      provides: "Source-centric catalog schema definitions"
      min_lines: 40
    - path: "src/catalog/load-catalog.ts"
      provides: "Catalog loading and validation entrypoint"
      min_lines: 30
    - path: "tests/catalog/source-catalog.test.ts"
      provides: "Catalog parsing and enable-disable coverage"
      min_lines: 30
  key_links:
    - from: "src/catalog/load-catalog.ts"
      to: "src/catalog/schemas.ts"
      via: "schema validation"
      pattern: "schema|safeParse|parse"
    - from: "tests/catalog/source-catalog.test.ts"
      to: "src/catalog/load-catalog.ts"
      via: "fixture-backed catalog loading"
      pattern: "loadCatalog"
---

<objective>
Implement the source-centric YAML catalog layer for external skills and plugins.

Purpose: Give the project a real configuration model for third-party sources before target-specific logic exists.
Output: Validated schemas, a loader, example catalog files, and tests for enable/disable behavior.
</objective>

<execution_context>
@C:/Users/AImagician/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/AImagician/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-foundation/01-CONTEXT.md
@.planning/phases/01-catalog-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define source-centric schemas for skill and plugin catalogs</name>
  <files>src/catalog/source-types.ts, src/catalog/schemas.ts</files>
  <action>Create the typed model and validation schemas for directory-based YAML catalogs. Support GitHub and command-based sources, keep entries source-centric, and include enabled/disabled state without inventing install behavior yet.</action>
  <verify>npm run typecheck</verify>
  <done>The project has a single validated shape for external source records and catalog sections.</done>
</task>

<task type="auto">
  <name>Task 2: Implement catalog loading and filtering</name>
  <files>src/catalog/load-catalog.ts</files>
  <action>Load YAML files from the catalog roots, validate them, and expose a normalized in-memory representation that excludes disabled sources while preserving enough metadata for later phases. Keep the implementation repository-focused, not adapter-focused.</action>
  <verify>npm test -- source-catalog</verify>
  <done>Catalog files can be parsed and disabled entries are filtered predictably.</done>
</task>

<task type="auto">
  <name>Task 3: Add example catalogs and fixture tests</name>
  <files>catalog/skills/example.yaml, catalog/plugins/example.yaml, tests/catalog/source-catalog.test.ts</files>
  <action>Create representative YAML examples for skills and plugins plus tests covering GitHub sources, command sources, and the enable/disable flag. Keep examples small but realistic enough for future planning and onboarding.</action>
  <verify>npm test</verify>
  <done>The catalog layer is backed by executable examples and regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm test -- source-catalog` passes
- [ ] `npm test` passes
</verification>

<success_criteria>

- All tasks completed
- External sources can be declared without hand-editing code
- GitHub and command-based sources share one validated model
- Disabled sources stay in config but are excluded from active resolution
</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-foundation/01-02-SUMMARY.md`
</output>
