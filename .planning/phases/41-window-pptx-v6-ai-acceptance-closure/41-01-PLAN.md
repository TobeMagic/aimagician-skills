# Phase 41: AI-Only Acceptance and Closure - Plan 01

**Requirements:** V6-PORT-02, V6-EVID-01, V6-DOC-01, V6-UAT-01,
V6-AUDIT-01, V6-REL-01
**Status:** Accepted

## Objective

Close v6 only after deterministic delivery evidence and three independent
fresh-context visual reviewers accept all three flagships.

## Non-Goals

- Do not lower thresholds, manually override a score, or conceal failed rounds.
- Do not use COM availability as a release gate.

## Tasks

1. Build anonymous hash-bound evidence with physically labeled calibration and
   candidate images.
2. Run three independent visual contexts with art-direction, narrative, and
   production lenses under one frozen rubric.
3. Validate reviewer identity, image hashes, score coverage, thresholds,
   reference parity, and consensus findings.
4. Run complete deterministic regression shards and workflow gates.
5. Obtain fresh OpenCode specification, quality, verification, and completion
   audits; resolve all Blocker/Important findings.
6. Update Skill documentation, phase summaries, ROADMAP, STATE, requirements,
   milestone status, commits, and push only after GO.

### Task 5: Independent audit and release (V6-AUDIT-01, V6-REL-01)

**Requirements:** V6-AUDIT-01, V6-REL-01

Run fresh OpenCode completion evidence mapping, retain the audit record, and
keep release fail-closed until every deterministic and visual gate passes.

## Verification

```bash
python -m pytest -q tests/window_pptx
node /home/aimagician/.codex/skills/aimagician-superpower/scripts/workflow.mjs validate \
  --project /mnt/d/growth_up_youth/repo/skills --phase 41 --gate execute
python skills/owned/window-pptx/scripts/check_window_pptx_private_assets.py \
  --staged --repo-root .
git diff --check
```

## Rollback And Recovery

Every review round is immutable. Invalid or unavailable rounds remain
diagnostic evidence and are never promoted. Release state changes only after a
fresh successful aggregate and completion audit.
