# Phase 39: Work-Report Flagship Tracer - Plan 01

**Requirements:** V6-DESIGN-01, V6-DECK-01, V6-PORT-01, V6-QA-01, V6-EVID-01
**Status:** Accepted

## Objective

Generate, inspect, render, and visually accept a 28-main plus 4-appendix
annual work report at the authorized reference deck's art-direction level.

## Non-Goals

- Do not add unsupported facts or commercial assets.
- Do not require COM or use raster slides.
- Do not close the phase from engineering checks alone.

## Tasks

### Task 1: Native flagship renderer

Implement the complete anatomy, certified spine metadata, notes lineage,
semantic charts/tables/diagrams, and three distinct case-study treatments.

### Task 2: Deterministic verification

Assert exact slide count, notes, native chart/table floor, no external
relationships, no slide pictures, manifest hashes, and locked brief lineage.

### Task 3: Portable visual proof

Render with an isolated LibreOffice profile, rasterize every page, create a
contact sheet, inspect key slides, and retain all exact candidate hashes.

### Task 4: Independent acceptance

Run fresh-context visual reviewers against anonymous evidence, repair concrete
Blocker/Important findings, and repeat until the Phase 41 aggregate passes.

## Verification

```bash
node --check skills/owned/window-pptx/scripts/build_window_pptx_v6_flagships.mjs
python -m pytest -q tests/window_pptx/test_v6_flagship_generator.py
node /home/aimagician/.codex/skills/aimagician-superpower/scripts/workflow.mjs validate \
  --project /mnt/d/growth_up_youth/repo/skills --phase 39 --gate execute
git diff --check
```

## Rollback And Recovery

The renderer is additive. Each accepted evidence round is immutable under its
own `phase39-40-flagships-rN` directory; failed visual rounds stay preserved
and never replace the latest accepted candidate.
