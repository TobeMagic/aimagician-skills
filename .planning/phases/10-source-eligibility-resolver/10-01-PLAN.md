---
wave: 1
depends_on: []
files_modified:
  - src/config/user-config.ts
  - src/manager/eligibility.ts
  - src/manager/manager.ts
  - tests/config/user-config.test.ts
  - tests/manager/eligibility.test.ts
  - tests/manager/manager-operations.test.ts
autonomous: true
requirements: [ELIG-01, ELIG-02, ELIG-03, ELIG-04, ELIG-05, ELIG-06]
---

# Plan 10-01: Source eligibility and explanation resolver

<must_haves>
- Sources can be visible/searchable but default-disabled for bulk install.
- `include` makes an individual skill eligible from a default-disabled source.
- `exclude` blocks install regardless of other rules.
- Command source install eligibility is global-only and explains project-scope skips.
- Manager search exposes eligibility status and reasons.
</must_haves>

<tasks>
<task>
<name>Add scoped include/exclude config schema</name>
<read_first>src/config/user-config.ts, tests/config/user-config.test.ts</read_first>
<action>Add `includedIds: string[]` and `excludedIds: string[]` to `UserSkillConfig`, default config, YAML schema loading, and scoped/non-scoped YAML round trips.</action>
<acceptance_criteria>`npm test -- tests/config/user-config.test.ts` exits 0 and tests assert included/excluded IDs persist.</acceptance_criteria>
</task>
<task>
<name>Add eligibility resolver</name>
<read_first>src/model/scopes.ts, src/bootstrap/plan-bootstrap.ts, src/manager/manager.ts</read_first>
<action>Create `src/manager/eligibility.ts` exporting `resolveSkillEligibility` with reason strings for source enabled/default-disabled, include/exclude priority, and command-source-global-only.</action>
<acceptance_criteria>`tests/manager/eligibility.test.ts` covers exclude strongest, include from default-disabled source, visible skipped default-disabled source, and command project skip.</acceptance_criteria>
</task>
<task>
<name>Expose eligibility in manager records and install skips</name>
<read_first>src/manager/manager.ts, tests/manager/manager-operations.test.ts</read_first>
<action>Add eligibility fields to `ManagerSkillRecord`, keep ineligible skills searchable, and skip install of blocked/ineligible selected skills with resolver reasons.</action>
<acceptance_criteria>`npm test -- tests/manager/eligibility.test.ts tests/manager/manager-operations.test.ts` exits 0.</acceptance_criteria>
</task>
</tasks>

<verification>
Run `npm run typecheck`, `npm test -- tests/config/user-config.test.ts tests/manager/eligibility.test.ts tests/manager/manager-operations.test.ts`, and `git diff --check`.
</verification>
