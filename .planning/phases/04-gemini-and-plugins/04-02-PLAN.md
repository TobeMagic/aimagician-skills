---
phase: 04-gemini-and-plugins
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/bootstrap/plan-bootstrap.ts
  - src/bootstrap/run-bootstrap.ts
  - src/model/targets.ts
  - tests/catalog/target-mapping.test.ts
  - tests/bootstrap/bootstrap-engine.test.ts
autonomous: true
requirements:
  - PLUG-01
  - PLUG-03
user_setup: []
must_haves:
  truths:
    - "Plugin assets stay distinct from skill assets all the way through planning and apply"
    - "Bootstrap can explain why a target/plugin combination was skipped instead of silently ignoring it"
    - "Capability-aware plugin decisions are testable from normalized target state through bootstrap reports"
  artifacts:
    - path: "src/bootstrap/plan-bootstrap.ts"
      provides: "Plugin-aware planned asset/report preparation"
      min_lines: 70
    - path: "src/bootstrap/run-bootstrap.ts"
      provides: "Skip-aware plugin reporting"
      min_lines: 110
    - path: "tests/bootstrap/bootstrap-engine.test.ts"
      provides: "Plugin report coverage"
      min_lines: 60
  key_links:
    - from: "src/bootstrap/run-bootstrap.ts"
      to: "src/model/targets.ts"
      via: "capability-based skip reasoning"
      pattern: "capab|skip|plugin"
    - from: "tests/catalog/target-mapping.test.ts"
      to: "src/bootstrap/plan-bootstrap.ts"
      via: "plugin target normalization expectations"
      pattern: "plugin"
---

<objective>
Make plugin handling capability-aware in planning and reporting before concrete installers land.

Purpose: Ensure plugin assets are visible in bootstrap planning and every unsupported or deferred target combination produces an explicit reason.
Output: plugin-aware plan structures, capability-driven skip reasoning, and tests proving the project never drops plugin assets silently.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gemini-and-plugins/04-CONTEXT.md
@.planning/phases/04-gemini-and-plugins/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread plugin assets through bootstrap planning</name>
  <files>src/bootstrap/plan-bootstrap.ts, src/model/targets.ts</files>
  <action>Update the bootstrap plan model so plugin assets remain visible with target-specific capability metadata and do not disappear just because Phase 3 only installed skills.</action>
  <verify>npm test -- target-mapping</verify>
  <done>Plugin assets are represented in planning with explicit capability-aware target state.</done>
</task>

<task type="auto">
  <name>Task 2: Add skip-aware plugin reporting</name>
  <files>src/bootstrap/run-bootstrap.ts, tests/bootstrap/bootstrap-engine.test.ts</files>
  <action>Extend bootstrap reporting so plugin work can surface installed, deferred, or skipped outcomes with explicit reasons per target.</action>
  <verify>npm test -- bootstrap</verify>
  <done>Bootstrap results can explain plugin skips and deferred automation explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Add mapping coverage for plugin capability paths</name>
  <files>tests/catalog/target-mapping.test.ts</files>
  <action>Add normalization tests covering plugin assets, per-target capability overrides, and reason propagation into later bootstrap stages.</action>
  <verify>npm test -- target-mapping</verify>
  <done>Plugin capability decisions are guarded by normalization tests before installers are added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- target-mapping` passes
- [ ] `npm test -- bootstrap` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/04-gemini-and-plugins/04-02-SUMMARY.md`
</output>
