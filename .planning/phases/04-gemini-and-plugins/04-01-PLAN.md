---
phase: 04-gemini-and-plugins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bootstrap/target-homes.ts
  - src/bootstrap/run-bootstrap.ts
  - src/bootstrap/source-resolution.ts
  - src/bootstrap/manifest.ts
  - tests/bootstrap/direct-target-sync.test.ts
autonomous: true
requirements:
  - TARG-04
user_setup: []
must_haves:
  truths:
    - "Bootstrap resolves a real Gemini user-level install home on Windows and Linux instead of deferring Gemini"
    - "Repository-managed skills targeted to Gemini become Gemini-native extension installs during bootstrap apply"
    - "Gemini installs are tracked in the manifest so reruns remain idempotent"
  artifacts:
    - path: "src/bootstrap/target-homes.ts"
      provides: "Gemini target-home resolution"
      min_lines: 40
    - path: "src/bootstrap/run-bootstrap.ts"
      provides: "Gemini apply/report integration"
      min_lines: 90
    - path: "tests/bootstrap/direct-target-sync.test.ts"
      provides: "Gemini isolated-home coverage"
      min_lines: 80
  key_links:
    - from: "src/bootstrap/run-bootstrap.ts"
      to: "src/bootstrap/source-resolution.ts"
      via: "Gemini-native source install preparation"
      pattern: "gemini"
    - from: "tests/bootstrap/direct-target-sync.test.ts"
      to: "src/bootstrap/run-bootstrap.ts"
      via: "Gemini apply verification"
      pattern: "gemini"
---

<objective>
Land Gemini as a real target by generating Gemini-native extension installs from repository-managed skill assets.

Purpose: Replace the current deferred Gemini placeholder with a deterministic user-level install path that matches Gemini CLI's documented extension model.
Output: Gemini home resolution, generated extension installs, manifest-backed apply behavior, and fake-home tests proving bootstrap writes Gemini-compatible output.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gemini-and-plugins/04-CONTEXT.md
@.planning/phases/04-gemini-and-plugins/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gemini target-home and install shape support</name>
  <files>src/bootstrap/target-homes.ts, src/bootstrap/manifest.ts</files>
  <action>Extend the current target-home and manifest model so Gemini has a first-class user-level destination and managed install records can describe non-skill-folder installs safely.</action>
  <verify>npm run typecheck</verify>
  <done>The bootstrap layer can model Gemini as a real managed target instead of a deferred placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Gemini-native installs from skill assets</name>
  <files>src/bootstrap/source-resolution.ts, src/bootstrap/run-bootstrap.ts</files>
  <action>Teach bootstrap to turn owned and GitHub-backed skill assets selected for Gemini into Gemini-native extension installs and include those results in apply reporting.</action>
  <verify>npm test -- direct-target-sync</verify>
  <done>Gemini receives generated extension installs from repository-managed skills during bootstrap.</done>
</task>

<task type="auto">
  <name>Task 3: Add isolated Gemini bootstrap coverage</name>
  <files>tests/bootstrap/direct-target-sync.test.ts</files>
  <action>Add fake-home coverage proving Gemini no longer reports as deferred and that generated extension installs land in the isolated Gemini home with the expected native files.</action>
  <verify>npm test -- direct-target-sync</verify>
  <done>Gemini target behavior is covered by executable fake-home tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm test -- direct-target-sync` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/04-gemini-and-plugins/04-01-SUMMARY.md`
</output>
