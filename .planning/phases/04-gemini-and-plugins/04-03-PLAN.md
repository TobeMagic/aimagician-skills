---
phase: 04-gemini-and-plugins
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/bootstrap/run-bootstrap.ts
  - src/bootstrap/direct-target-sync.ts
  - src/bootstrap/target-homes.ts
  - src/bootstrap/manifest.ts
  - tests/bootstrap/direct-target-sync.test.ts
  - tests/bootstrap/bootstrap-smoke.test.ts
autonomous: true
requirements:
  - PLUG-02
  - PLUG-03
user_setup: []
must_haves:
  truths:
    - "Plugin assets install only for targets with a stable native automation path"
    - "OpenCode receives repository-managed plugin assets in its user-level plugin home"
    - "Unsupported plugin targets are skipped with explicit reasons instead of failing silently"
  artifacts:
    - path: "src/bootstrap/run-bootstrap.ts"
      provides: "Plugin apply integration and reporting"
      min_lines: 130
    - path: "src/bootstrap/direct-target-sync.ts"
      provides: "Managed plugin directory sync support"
      min_lines: 80
    - path: "tests/bootstrap/direct-target-sync.test.ts"
      provides: "Plugin install and skip coverage"
      min_lines: 110
  key_links:
    - from: "src/bootstrap/run-bootstrap.ts"
      to: "src/bootstrap/direct-target-sync.ts"
      via: "plugin install application"
      pattern: "plugin"
    - from: "tests/bootstrap/bootstrap-smoke.test.ts"
      to: "src/bootstrap/run-bootstrap.ts"
      via: "bootstrap plugin end-to-end behavior"
      pattern: "plugin|skip"
---

<objective>
Install plugin assets for supported targets and finalize explicit skip behavior for unsupported ones.

Purpose: Turn plugin metadata into real user-level installs where the target exposes a stable contract, while preserving honest skip reporting for the rest.
Output: managed OpenCode plugin installs, manifest-backed rerun behavior, and end-to-end tests covering both installation and skip cases.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gemini-and-plugins/04-CONTEXT.md
@.planning/phases/04-gemini-and-plugins/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add plugin-capable target homes and managed sync support</name>
  <files>src/bootstrap/target-homes.ts, src/bootstrap/direct-target-sync.ts, src/bootstrap/manifest.ts</files>
  <action>Extend the managed sync model so plugin installs can write into target-native homes and be updated or pruned safely across reruns.</action>
  <verify>npm run typecheck</verify>
  <done>The bootstrap engine can model plugin installs as managed target-native directories.</done>
</task>

<task type="auto">
  <name>Task 2: Install supported plugin assets and skip the rest explicitly</name>
  <files>src/bootstrap/run-bootstrap.ts, tests/bootstrap/direct-target-sync.test.ts</files>
  <action>Implement repository-managed plugin installs for supported targets such as OpenCode and ensure unsupported targets report explicit skip reasons instead of failing or disappearing.</action>
  <verify>npm test -- direct-target-sync</verify>
  <done>Plugin assets install where supported and are skipped transparently elsewhere.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end bootstrap smoke coverage for plugin behavior</name>
  <files>tests/bootstrap/bootstrap-smoke.test.ts</files>
  <action>Extend the built-bootstrap smoke test so plugin installs and skip reporting remain covered in the compiled CLI path, not just unit-level apply tests.</action>
  <verify>npm test -- bootstrap-smoke</verify>
  <done>The compiled CLI path proves plugin behavior end to end in isolated fake homes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm test -- direct-target-sync` passes
- [ ] `npm test -- bootstrap-smoke` passes
- [ ] `npm test` passes
</verification>

<output>
After completion, create `.planning/phases/04-gemini-and-plugins/04-03-SUMMARY.md`
</output>
