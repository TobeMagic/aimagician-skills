---
phase: 12-tui-orchestration-console
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/manager/manager.ts
  - src/tui/dashboard.ts
  - src/tui/source-toggle.ts
  - tests/manager/manager-operations.test.ts
  - tests/tui/source-toggle.test.ts
autonomous: true
requirements:
  - TUI3-01
  - TUI3-02
  - TUI3-03
  - TUI3-04
  - TUI3-05
must_haves:
  truths:
    - "TUI can display scope-specific source status, eligibility status, preview, and final report information."
    - "Source controls persist scoped override YAML and support enabled/default-disabled/disabled semantics as far as current config model allows."
    - "Skill include/exclude controls persist scoped override YAML and immediately affect eligibility/search/install behavior."
    - "Install/sync flow shows a pre-execution preview modal before filesystem writes."
    - "Final Target x Skill report remains visible after sync and distinguishes installed/skipped/removed/overwritten where available."
  artifacts:
    - path: "src/manager/manager.ts"
      provides: "Scope-aware preview/report data for TUI orchestration"
      contains: "previewInstallSkills"
    - path: "src/tui/source-toggle.ts"
      provides: "Tri-state source row formatting and overlay helper"
      contains: "default-disabled"
    - path: "src/tui/dashboard.ts"
      provides: "TUI orchestration controls, preview modal, include/exclude keys, final report"
      contains: "Sync Preview"
    - path: "tests/manager/manager-operations.test.ts"
      provides: "Manager preview and include/exclude persistence coverage"
      contains: "preview"
    - path: "tests/tui/source-toggle.test.ts"
      provides: "Pure source row formatting coverage"
      contains: "default-disabled"
---

<objective>
Implement Phase 12 by extending the existing blessed Hive/Cells/Nectar TUI into a scope-aware orchestration console for source state, include/exclude state, preview confirmation, eligibility visibility, and final reporting.
</objective>

<execution_context>
@/home/aimagician/.claude/get-shit-done/workflows/execute-plan.md
@/home/aimagician/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-tui-orchestration-console/12-CONTEXT.md
@.planning/phases/12-tui-orchestration-console/12-UI-SPEC.md
@.planning/phases/12-tui-orchestration-console/12-RESEARCH.md
@src/tui/dashboard.ts
@src/tui/source-toggle.ts
@src/manager/manager.ts
@src/config/user-config.ts
@tests/manager/manager-operations.test.ts

Current code already has `sourceOverrides: Record<string, boolean>`, `includedIds`, and `excludedIds` in `UserSkillConfig`. Do not replace them with a stale object schema. Use booleans for source overrides: `true` enabled, `false` disabled/default-disabled override; catalog `source.enabled` remains the baseline default state.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add manager preview and eligibility controls for the TUI</name>
  <read_first>
    - src/manager/manager.ts
    - src/config/user-config.ts
    - src/bootstrap/direct-target-sync.ts
    - tests/manager/manager-operations.test.ts
  </read_first>
  <files>src/manager/manager.ts, tests/manager/manager-operations.test.ts</files>
  <behavior>
    - Manager exposes a non-mutating preview API for selected asset IDs and selected targets.
    - Preview returns scope, workspaceRoot, manifestPath, operations, skipped reasons, and target reports sufficient for TUI preview modal.
    - Manager exposes scoped helpers to set include/exclude/default override state for selected skills.
    - Existing `installSkills` behavior remains compatible and still honors include/exclude/source eligibility.
  </behavior>
  <action>
    In `src/manager/manager.ts`, import `planManagedInstallSync` and `ManagedInstallSyncOperation` from `../bootstrap/direct-target-sync`. Export `PreviewInstallSkillsResult` with fields `scope`, `workspaceRoot`, `manifestPath`, `operations: ManagedInstallSyncOperation[]`, `skipped: Array<{ assetId: string; target?: SupportedTarget; reason: string }>`, and `targetReports: Array<{ target: SupportedTarget; plannedSkillIds: string[]; skippedSkillIds: string[]; removeSkillIds: string[]; overwriteSkillIds: string[] }>`. Add `export async function previewInstallSkills(options: InstallSkillsOptions): Promise<PreviewInstallSkillsResult>` that mirrors `installSkills` up to eligibility filtering and managed install resolution but never calls `syncManagedInstalls`, `writeManifest`, `executeCommandSkillSources`, `mkdir`, `rm`, or `cp`; it calls `planManagedInstallSync` with `allowedRootsByTarget`, selected targets, eligible managed installs, and previous manifest managed installs. Add exported `setSkillOverride(options: ManagerBaseOptions & { assetIds: string[]; state: "include" | "exclude" | "default" }): Promise<UserSkillConfig>` that loads scoped config, updates `includedIds` and `excludedIds` sets, saves scoped config, and returns config. Add tests in `tests/manager/manager-operations.test.ts` that `previewInstallSkills` does not write target files and that `setSkillOverride` persists include/exclude/default in scoped YAML.
  </action>
  <verify>
    <automated>npm test -- tests/manager/manager-operations.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function previewInstallSkills" src/manager/manager.ts` returns a match.
    - `grep -n "ManagedInstallSyncOperation" src/manager/manager.ts` returns a match.
    - `grep -n "export async function setSkillOverride" src/manager/manager.ts` returns a match.
    - `grep -n "previewInstallSkills" tests/manager/manager-operations.test.ts` returns a match.
    - Manager tests and typecheck pass.
  </acceptance_criteria>
  <done>Manager provides the non-mutating preview and include/exclude controls needed by the TUI.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire TUI source, include/exclude, preview, and report overlays</name>
  <read_first>
    - src/tui/dashboard.ts
    - src/tui/source-toggle.ts
    - src/tui/theme.ts
    - src/manager/manager.ts
    - tests/tui/group-filter.test.ts
  </read_first>
  <files>src/tui/dashboard.ts, src/tui/source-toggle.ts, tests/tui/source-toggle.test.ts</files>
  <behavior>
    - Source overlay rows show enabled/default-disabled/disabled labels and persist scoped config through existing save path.
    - Cells/Nectar display eligibility/source/include/exclude hints using compact strings.
    - `I`, `X`, and `d` set include, exclude, and default override for selected/highlighted skills.
    - Install key `i` opens `Sync Preview` modal first; Enter confirms and then runs install, Esc cancels without writes.
    - Final report modal uses `Sync Complete` title and target grouping.
  </behavior>
  <action>
    In `src/tui/source-toggle.ts`, add pure exported helpers `sourceStateLabel(sourceEnabled: boolean, override: boolean | undefined): "enabled" | "default-disabled" | "disabled"` and `renderSourceStateRow(source, userConfig, currentColors)` or equivalent, ensuring row strings contain exact labels `enabled`, `default-disabled`, and `disabled`. Preserve `showSourceToggle` export and update its row rendering/hint to `space cycle  enter save  esc cancel`; if full tri-state cannot persist separately with boolean overrides, implement cycle as enabled/default-disabled using `undefined` baseline and disabled using `false`, documenting exact boolean mapping in code identifiers not comments.

    In `src/tui/dashboard.ts`, import `previewInstallSkills` and `setSkillOverride`. Extend command help and guide bar with exact keys `p` Preview Sync, `I` include, `X` exclude, `d` default. Add helper `getActionAssetIds()` if needed to reuse selected/highlighted IDs. Add `showSyncPreview(assetIds, targetsChoice)` that calls `previewInstallSkills`, renders a centered blessed modal titled `Sync Preview` with summary counts for create/overwrite/remove/skip and grouped operation rows, and resolves `true` on Enter and `false` on Esc/q without writes. Update install mutation so it calls `showSyncPreview` before `installSkills`; if preview returns false, log `Sync cancelled.` and do not call `installSkills`. Add `p` key to preview only. Add key handlers `I`, `X`, `d` that call `setSkillOverride` with `include`, `exclude`, `default`, refresh, and log the new state. Update detail rendering to include source ID and eligibility/source override summary when available from `ManagerSkillRecord` fields; use existing available fields if deeper reason fields are not available. Add `tests/tui/source-toggle.test.ts` for pure state label/row helpers.
  </action>
  <verify>
    <automated>npm test -- tests/tui/source-toggle.test.ts tests/tui/group-filter.test.ts tests/manager/manager-operations.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Sync Preview" src/tui/dashboard.ts` returns a match.
    - `grep -n "Sync Complete" src/tui/dashboard.ts` returns a match.
    - `grep -n "previewInstallSkills" src/tui/dashboard.ts` returns a match.
    - `grep -n "setSkillOverride" src/tui/dashboard.ts` returns a match.
    - `grep -n "default-disabled" src/tui/source-toggle.ts tests/tui/source-toggle.test.ts` returns matches.
    - Targeted TUI/manager tests and typecheck pass.
  </acceptance_criteria>
  <done>TUI exposes scope-aware orchestration controls, preview confirmation, eligibility hints, and final report UX on existing blessed dashboard.</done>
</task>

</tasks>

<verification>
Run:
```bash
npm test -- tests/manager/manager-operations.test.ts tests/tui/source-toggle.test.ts tests/tui/group-filter.test.ts
npm run typecheck
```
</verification>

<success_criteria>
- TUI3-01: Scope-specific preview/report/status data is visible through manager-backed TUI state.
- TUI3-02: Include/exclude controls persist to scoped override YAML.
- TUI3-03: Source state and eligibility/status appear in TUI rows/details.
- TUI3-04: Pre-execution `Sync Preview` modal appears before writes.
- TUI3-05: Final `Sync Complete` report shows target-grouped operation results.
</success_criteria>

<output>
After completion, create `.planning/phases/12-tui-orchestration-console/12-01-SUMMARY.md`.
</output>
