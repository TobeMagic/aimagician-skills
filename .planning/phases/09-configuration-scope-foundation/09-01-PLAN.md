---
wave: 1
depends_on: []
files_modified:
  - src/config/user-config.ts
  - src/manager/manager.ts
  - tests/config/user-config.test.ts
  - tests/manager/manager-operations.test.ts
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
---

# 09-01: Scope-aware config and manifest foundation

## Goal

Implement scope-aware user override YAML paths and wire manager operations to use the current scope while preserving existing independent manifest behavior.

## Tasks

<task id="1">
<action>Add `ScopedUserConfigOptions`, `scopedUserConfigPath`, `loadScopedUserConfig`, and `saveScopedUserConfig` to `src/config/user-config.ts`. Global path must be `<configBaseDir>/skillbee/global/config.yaml`; project path must be `<projectDir>/.skillbee/config.yaml` or `process.cwd()/.skillbee/config.yaml` when no projectDir is supplied.</action>
<read_first>
- src/config/user-config.ts
- tests/config/user-config.test.ts
</read_first>
<acceptance_criteria>
- `tests/config/user-config.test.ts` contains `resolves global and project override config paths independently`
- `tests/config/user-config.test.ts` contains `loads and saves global and project override configs without cross-scope interference`
- `npm test -- tests/config/user-config.test.ts` exits 0
</acceptance_criteria>
</task>

<task id="2">
<action>Update manager config reads/writes so `searchSkills` and `archiveSkills` use `loadScopedUserConfig` / `saveScopedUserConfig` with `configBaseDir`, `scope`, and `projectDir` from the current operation.</action>
<read_first>
- src/manager/manager.ts
- tests/manager/manager-operations.test.ts
</read_first>
<acceptance_criteria>
- `src/manager/manager.ts` contains `loadScopedUserConfig({`
- `src/manager/manager.ts` contains `saveScopedUserConfig({`
- `tests/manager/manager-operations.test.ts` contains `keeps archive overrides isolated between project and global scopes`
- `npm test -- tests/manager/manager-operations.test.ts` exits 0
</acceptance_criteria>
</task>

## Verification

- `npm run typecheck`
- `npm test -- tests/config/user-config.test.ts tests/manager/manager-operations.test.ts`

## Must Haves

- Global override config path is `~/.config/skillbee/global/config.yaml` via `configBaseDir`.
- Project override config path is `<project>/.skillbee/config.yaml`.
- Project and global user override config state remain isolated.
- Manager durable config changes use scoped YAML and do not mutate repo catalog/taxonomy defaults.
