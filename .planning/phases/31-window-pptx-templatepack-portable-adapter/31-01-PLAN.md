# Phase 31 Plan 01: Trusted Masks and Masked Similarity

**Status:** Accepted
**Requirements:** P31-MASK-01, P31-SIM-01, V51-TPL-03

1. Add failing tests for nested group transforms, chart-frame resolution,
   deterministic masks, invalid/excessive masks, in-mask change, and
   out-of-mask change.
2. Add the additive TemplatePack visual-mask schema and loader contract.
3. Implement source-only OOXML geometry extraction with affine transform
   composition and relationship-based chart resolution.
4. Implement the Pillow scorer and versioned report.
5. Generate and review the institutional pack's trusted masks.
6. Run focused tests and `git diff --check`.

**Checkpoint:** a source/candidate PNG pair can receive a fail-closed
engineering similarity verdict without inspecting their differences to build
the mask.
