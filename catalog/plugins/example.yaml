sources:
  - id: opencode-browser
    type: command
    enabled: true
    command:
      run: opencode plugin add browser-tools
    targets:
      include:
        - opencode
    assets:
      - id: browser-tools
        kind: plugin
        description: Example plugin entry kept separate from skills
